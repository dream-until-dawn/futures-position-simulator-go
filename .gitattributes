# 全库行尾：**LF**。⚠️ 2026-09-10 使用者裁决。
#
# 为什么定下来、而不是「先放着」：
#
#   仓库此前存的  CRLF，317/317，而**没有任何一处写下过这个约定**
#   gofmt 的输出  ⚠️ **LF** —— 每个 Go 文件、每次 `gofmt -w` 都翻一份
#   守卫          对「整份翻成 LF」是瞎的（它只查「混用」与「多余回车符」）
#
# ⇒ 「没有约定」不等于「中立」：两个候选里有一个**每次格式化都在推进**，
#   另一个只靠「没人跑 gofmt -w」。**先放着 = 归一到 LF 的慢速版本，只是没人签字。**
#
# ⚠️ 选 LF 而不是 CRLF 的理由是**与 Go 工具链一致**：选 CRLF 意味着
# 每个人每次 `gofmt -w` 之后都要记得还原 —— 那是一条纪律，而本仓库
# 反复证明过纪律挡不住（silent-risks.md 66）。
#
# ⚠️ 已核：全库**没有任何 .bat / .cmd / .ps1**，没有文件真需要 CRLF。
#
# 详见 eol_test.go 的说明与 docs/silent-risks.md。
* text=auto eol=lf
