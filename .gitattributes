# 全库行尾：**LF**。⚠️ 2026-09-10 使用者裁决。
#
# 真实局面（⚠️ 字节级核过；此前我报过一版**错的**，见文末）：
#
#   git 对象库里存的   **一直是 LF** —— `core.autocrlf=true` 在 `git add` 时就归一了
#   工作树             **CRLF** —— 同一个 autocrlf 在 checkout 时又铺回去
#   gofmt 的输出       **LF** —— 每个 Go 文件、每次 `gofmt -w` 都翻工作树里那一份
#   换行守卫           读的是**工作树**
#
# ⇒ 于是此前那条守卫**量的是本机 git 配置的产物，不是仓库的内容** ——
#   同一个提交在 `autocrlf=false` 的机器上检出会是 LF，那条守卫看到的东西完全不同。
#   ⚠️ **一条判据依赖每台机器的本地配置，而它自己不知道这一点。**
#
# ⇒ 写下 `eol=lf` 的作用不是「改变仓库存什么」（它本来就是 LF），
#   是**让工作树与对象库一致**，从而：
#     ① 守卫看到的与仓库里的是同一个东西；
#     ② `gofmt -w` 不再制造工作树与索引的差异；
#     ③ 不再依赖每台机器碰巧怎么配 `core.autocrlf`。
#
# ⚠️ 选 LF 而不是 CRLF：LF 是对象库里本来就有的形态，也与 Go 工具链一致。
# 选 CRLF 要同时对抗 gofmt 与 autocrlf 两边，那是一条纪律，
# 而本仓库反复证明过纪律挡不住（silent-risks.md 66）。
#
# ⚠️ 已核：全库**没有任何 .bat / .cmd / .ps1**，没有文件真需要 CRLF。
#
# 详见 eol_test.go 的说明与 docs/silent-risks.md。
* text=auto eol=lf
