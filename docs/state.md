# 项目状态：唯一来源

⚠️ **本文件是项目状态与计数的唯一断言处。** 其余任何文档提到这些数字，
要么给链接，要么就是**复述**——而复述会过期，且过期时不会有任何动静。

这一页的存在理由，是 2026-09-07 连着栽的两次：

- 待实测项从 6 条变成 7 条，而「六条 / 6 条」还散在 5 份文档 13 处。
  ⚠️ 其中一处是 `roadmap.md` 的 **v0.1.0 验收行**——照那行字，
  v0.1.0 可以在实验 7 一次没做的情况下宣称达标。
- 持仓截面实测 54 字段，而三处仍写「28 字段」。
  ⚠️ 其中一处是 **v0.2.0 的三态验收行**——照那行字，
  新发现的 26 个字段整体不在验收范围里，包括
  `margin_long_today/his`、`open_cost_long_today/his`、`position_cost_long_today/his`
  这批**逐日盯市两条基线在柜台侧的形态**，也就是这个库最核心的东西。

两次都躲过了 `restatement-count.sh`，因为**计数不是状态词**。
状态词至少是词，人眼会停；**计数是数字，人眼扫过去根本不会停。**

---

## ✅ 那一手过夜仓已了结（自然日 2026-09-10 09:19），⚠️ **而它要办的那件事没办成**

    SHFE.rb2701  多头 1 手 @3146  →  09:19 平掉，占用保证金归零，账户干净

**按条款执行的**（「最迟自然日 2026-09-10 收盘前必平，无论量没量到」，使用者当日裁决维持原条款）。

### ⚠️ 没办成的是「昨仓保证金基准」，原因是**我的日期算术错了**

    自然日 2026-09-09 夜盘 ┐
                          ├─ 同属交易日 **20260910**
    自然日 2026-09-10 日盘 ┘

⇒ 交易日 20260910 的结算在**今天日盘收盘之后**，所以这条腿从建仓到平掉**全程是今仓**
（`PositionDate=1`、`YdPosition=0`），它要到交易日 **20260911** 才会变昨仓。
而条款里写的「最早：交易日 20260910 结算之后（**即自然日 2026-09-10 日盘开盘起**）」
括号里那句是错的 —— **按字面执行条款，这个实验必然跑不成。**

> ⚠️ **那条截止时间是从一个错的日期算术推出来的。**
> 一个从错误前提推出的截止时间，与一个经过判断的截止时间，**写下来长得一模一样** ——
> 而后者该守、前者该改。**分辨它们的唯一办法是回去核那个前提**，而当时没有人核
> （包括评审：他复核了条款的**形式**，没核那个日期）。

### 重做它需要什么（⚠️ 别再算错一次）

⚠️ **重做时要顺带补一张欠着的截面**：「最新价基准被否」这一条目前**只有散文、
没有夹具**（probes.md §6.10）—— 需要一张**有持仓且 `LastPrice ≠ 开仓价`** 的
带 `-quote` 截面。⚠️ 它必须**有持仓**才有 `UseMargin` 可比，所以只能搭着这次重做一起拍。

    建仓   在**某个交易日的日盘收盘之前**建一手
    等     ⚠️ 等它跨过**那个交易日的结算**（日盘收盘之后），
           也就是**下一个交易日**的夜盘或日盘才看得到昨仓
    量     oracle ctp-hold -symbol <合约> -rounds 1，看 UseMargin
    平     oracle ctp-flatten

**事前登记的两个候选照旧有效**（当时算的是 3164 那一天，重做时要按当时的昨结算价重算）：

    候选 A   UseMargin = PositionCost × 费率，而 PositionCost 结算时按逐日盯市重置
    候选 B   UseMargin 在建仓时按开仓价定死，之后不再重算

⚠️ **重做之前先写下那两个数**，理由不变：两个数差得不多，都长得像「合理的保证金」。

#### ✅ 事前登记（自然日 2026-09-10 12:41 写下，**开仓之前**）—— 使用者当日裁决「建，今晚拍完就平」

**日期算术这次核过了，连证据一起写下**（上一次就是这一步没核）：

    ctp-status-20260909.json    captured_at 2026-09-09T15:35  →  trading_day 20260909
    ctp-status-20260910-2.json  captured_at 2026-09-09T21:30  →  trading_day 20260910   ← 夜盘翻日
    ctp-status-20260910-5.json  captured_at 2026-09-10T09:04  →  trading_day 20260910

⇒ 今天（自然日 2026-09-10）日盘属于**交易日 20260910**，它的结算在今天 15:00 之后。
⇒ 今晚 21:00 的夜盘属于**交易日 20260911**，那一手届时是**昨仓**。

> ⚠️ **最后这一句是类推，不是对今晚的直接观测** —— 依据是前一天同一形态（09-09 夜盘 → 20260910）。
> 今晚连上柜台读一次 `TradingDay` 就能确证；**读到之前不许把它当已知**。

**条款**（这一次的「最迟」有依据，不是从错前提推出来的）：

    正常   今晚 21:00 夜盘拍全 ①②③，**拍完立刻平**
    兜底   若今晚拍不成（连不上 / 夜盘没开 / 字段没落下来），
           **最迟交易日 20260911 日盘收盘前必平**（自然日 2026-09-11 15:00）
    理由   ⚠️ 再留下去要跨第二次结算，而 ① 在**第一次**结算之后就已经答完 ——
           多留一夜换不到任何信息，只多担一夜风险

**两个候选的数**（rb 乘数 10，`MarginRateByMoney = 0.16`，实测自 `ctp-status-20260910-5`）：

    候选 A（逐日盯市重置）   UseMargin = 1.6 × S    S = 交易日 20260910 的结算价
                                                    （= 交易日 20260911 的 PreSettlementPrice）
    候选 B（开仓价定死）     UseMargin = 1.6 × P    P = 本次开仓价，建仓当场记下

⚠️ **只有 S ≠ P 时这一轮才有判别力。** 若两者相等，**什么都没分开，不许当结论** ——
这正是 `ctp-hold` 自己那句「若行情也没动 ⇒ 这一轮什么都没分开」的同一个陷阱，
而它在**跨结算**这一版上换了个外形：盘中比的是行情动没动，这里比的是结算价与开仓价差不差。

⚠️ 盘中那一段**已经证过 B 了**（`ctp-status-20260910-3`：`OpenCost 31460` /
`SettlementPrice 3141` / `UseMargin 5033.60` = 1.6×3146 —— 用结算价算不出这个数）。
① 问的是**跨过一次结算之后 B 还成不成立**，不是重复。

**命令**：

    建   oracle ctp-hold -symbol SHFE.rb2701 -keep
    拍   oracle ctp-hold   -symbol SHFE.rb2701 -rounds 1        ①
         oracle ctp-order  -close -dump testdata/ctp            ②  ⚠️ 这条代码路径**从没跑过**
         oracle ctp-params -dump testdata/ctp -quote SHFE.rb2701 ③  ⚠️ 要挑 LastPrice ≠ P 的时刻
    平   oracle ctp-flatten

### ⚠️ 下一次账上有仓时，**一次拍全**这三件 —— 别再分批

20260910 评审指出：下面三条欠账**全挂在同一个前提上**（账上有仓），
而它们今天漏网**全是因为当时手上的工具还没收那个字段**（silent-risks.md 79）。
⇒ **分批拍等于把同一个原因再犯两次。**

    ① 昨仓保证金基准     `ctp-hold -rounds 1`，看 UseMargin
       候选 A/B 见上一小节；⚠️ **重做时按当时的昨结算价重算那两个数，并事前写下**

    ② 平仓挂单的冻结     `ctp-order -close -dump testdata/ctp`
       ⚠️ 这是 `kq_facts` 42 升格的**唯一缺口**：那半句的证据
       （`FrozenMargin = 0`）此刻夹具里 **0 处**，只有散文
       ⚠️ 截面必须在**撤单之前**拍 —— `-dump` 已经保证，守卫 `TestHeldCaptureHappensBeforeCancel`

    ③ 最新价基准的否证   `ctp-params -dump … -quote <合约>`，
       ⚠️ 且要挑一个 **`LastPrice ≠ 开仓价`** 的时刻（否则判别力为**零**，见 probes.md §6.10）

⚠️ **开仓之前先做一件事**（评审 20260910）：**确认这一次要收的字段都收得到** ——
今天这三条漏网**全是因为工具当时没收那个字段**（silent-risks.md 79）。
⇒ 先空跑一次 `ctp-params -quote` 与 `ctp-order -dump`，看落盘的那份里
`LastPrice` / `FrozenMargin` / `FrozenCommission` / `UseMargin` 都在，**再开仓**。
⚠️ **事后发现字段没收到，那一次持仓就白留了。**

⚠️ **顺序**：①②③ 都不发成交单，可以任意先后；但**平仓要放在最后** ——
仓一平，②③ 就都拍不到了。

### ⚠️ 而这一轮**换来了三件当时没打算要的**

    §6.10  停盘区间的另一边补齐 ⇒ kq_facts 12 的 CTP 侧第二来源**完整**
    §6.10  ⚠️ 同一份截面里账户块与持仓块**自己不自洽**（差一个 tick 的盈亏）
    §6.11  平仓挂单的冻结 ⇒ kq_facts 42 的 CTP 侧第二来源，**同向**

⚠️ 第三件只有**账上有仓**时量得到 —— 它是趁平仓之前抢下来的。
**一个失败的实验没有白留仓**，但那不是留仓的理由：这三件里有两件不需要过夜。


## 计数

| 键 | 值 | 出处 |
|---|---|---|
| `rules_pending` | **13** | [cn-futures-rules.md](./cn-futures-rules.md) §13。⚠️ 交易日 20260908 的夜盘先从 7 涨到 10（新增三条全部由实测本身暴露），当晚补测掉一条；送审后核实评审的重新框定时又撞出两条，净 +4；⚠️ 自然日 2026-09-08 日盘的破坏验证又撞出两条（第 13/14 条），**两条都是「本口子上分不开」而不是「没建模」** —— 一条声称的盲区被破坏演示过一次，才算确认它存在 |
| `rules_wired` | `PositionDateType` | v0.3.0 起**接进 position**：`New` 带上它、`Settle` 按它分岔（`UseHistory` 滚今昨、`NoUseHistory` 只推进基线），零值**结算时**报错。⚠️ `New` 刻意不拦零值 —— 那会逼只重放、永不结算的调用方编一个值出来，而**一个逼人编数据的守卫比没有守卫更坏**：编出来的值会被后来的人当成实测值 |
| `packages_done` | `types` `position` `account` `pnl` `fee` `margin` `refdata` `refdata/live` `refdata/exchange` `conformance` `conformance/fixture` `conformance/ctpfixture` `view` `order` `internal/decimalx` | v0.1.0 已落地的包（`ctperr` 未开始） |
| `rules_measured` | **2** | 规则**已被两个独立来源同向证实**的条数。⚠️ 20260909 使用者裁决升格 `kq_facts` 37（逐日盯市基线的昨仓那一端用收盘价）后 1 → 2。升格判据见下节，它**同时否掉了**看起来最硬的第 20 条 —— 那条只有一个候选，对不上说明数据错了，不说明规则是另一条 |
| `rules_listed` | **15** | §13 一共问过几条（**含已收敛，只增不减**）。⚠️ 它是 20260909 新增的**第二个分母**，因为 `rules_pending` 只数还欠着的：一条被测掉的项会离开分母，于是「只记解决不记发现」时**分子涨、分母缩**，两个方向叠起来让比值凭空变好看。恒等式 `rules_listed = rules_pending + 已收敛` 由 `TestRulesListedMatchesTable` 钉住 |
| `reject_priority_measured` | **4** | 报单校验八项两两共 **28** 对，实测判出先后的对数。见下节。⚠️ **其余各处一律链接、不抄数** —— 这个数 20260909 当天从 3 涨到 4，而五处复述里只有一处跟上了。它说的正是「生产代码里有多少顺序是猜的」，是这批里最不该含糊的数字 |
| `kq_facts` | **51** | 关于**快期模拟这个口子**的已实测事实，见下节 |
| `account_fields` | **23** = 保留 **21** + 丢弃 **2** | [probes.md](./probes.md) §6.2（DIFF 文档只列 18）。⚠️ **这个数此前只写「23」，没说它数的是哪一种**：柜台给 23 个键，夹具里保留 21 个（另 2 个是标识性字段，白名单显式丢弃）。对拍要对齐的是**保留的 21 个** —— 拿 23 去做完整性断言会永远差 2 个而查不出为什么 |
| `conformance_position` | 见 `TestPositionViewAcrossAllFixtures` 的棘轮 | ⚠️ **数字刻意不抄在这里**：抄一份就要人手同步，而这个数每改进一个字段就会变一次，一处过期的「32/52」比没有更坏。测试里钉着当前值，退化与改进都会红 |
| `position_fields` | **54** | probes.md §6（DIFF 文档只列 28）。⚠️ 54 个已由 `view.Position` 逐个表态，完整性由 `TestPositionViewCoversMeasuredFields` 从**夹具**反推，不是手抄名单 |
| `fixture_orders` | 见 `TestEveryFixtureFrozenParses` 的报数 | **记了委托的夹具份数**。⚠️ 20260909 之前是 **0** —— 委托此前根本不进夹具，于是柜台的 `volume_*_frozen_*` （它们是「挂着的委托」的函数）**拿什么去比都比不了**。破坏验证当场演示过：把冻结合计改成漏掉昨仓那部分，全套测试照样绿。⚠️ **20260910 更正：那次「演示」是空转的** —— 破坏 131 的 `test` 字段指着一个住在 `./view/` 的函数名，而它的 `pkg` 是 `./conformance/fixture/`，`-run` 锚定后选中零条测试、退出 0。实测现在 **19 个样本接上了冻结**，而 `TestPositionViewAcrossAllFixtures` **抓得住**那个破坏（`volume_long_frozen` 多出一个没登记的失败字段），131 已从 `expect=green` 改成 `expect=red`。⚠️ 这个数变大是好消息 |
| `position_evidence` | 见 `TestPositionFieldEvidence` 的棘轮 | 54 个字段里**有多少个从未取到过非零值**。⚠️ 数字同样不抄在这里，理由同上一行。⚠️ 但**分档要记住**：只见过零 / 只见过 `"-"` / 非数值字段是三种东西，下一步动作完全不同（造场景 / 本来就不该有数 / 压根不是数）。合起来数会让人去补两个补不上的缺口 —— 那正是这条测试拆开之前的样子 |

⚠️ **验收行不许抄这些数字**，一律写「全部待实测项」「全部字段」并链接本页。
抄一次就多一处会过期的复述，而它下一次过期时同样不会有动静。

## 下一步的三件，**顺序有理由**（20260910 收尾定）

    1  ⚠️ 给那 **3 条**无红搭档的 `expect=green` 各配一条红搭档
       —— 三件里**唯一能立刻做完**的，成本最低、边界最清楚
       ⇒ 做完这套对账**零盲点**
    2  数「已经成立但没有人数过」的性质（不依赖行情）
       ⚠️ 判据已在动手**之前**立好，见下一节
    3  三条共用「账上有仓」的观测 —— 等下一个交易日
       ⚠️ **开仓之前**先空跑确认那四个字段落得下来

⚠️ **1 必须排在 2 前面**（评审 20260910 提出，我采纳）：

> 做完 1 之后，2 产出的任何「性质」都会被**一套零盲点的对账**验着；
> 顺序反过来的话，**2 的成果要在一套还有三个盲点的体系里被信任**。

⚠️ 而这与本页那条「**先立判据再动手**」是同一个形状：
**都是把「验它的东西」放在「被验的东西」之前** ——
反过来的话，人不会用一套刚发现有洞的体系去质疑自己刚拿到的成果。

（⚠️ 新规矩下第九条落库的归属记录。）

## ⚠️ 「数出来的性质」进哪一栏 —— 先说死，免得明天好看

下一步打算**数一遍「这个仓库里已经成立、但没有人数过」的性质**
（起因：想推广 265/266 那个「一条划盲区、一条划能力」的做法时，
发现它**已经在 22 条破坏上默默生效了**，只是没人数过）。
⚠️ **20260910 更正：那个 22 是按 `test` 字段字符串相等数的，而它没被核过** —— 其中一条（131）配在一个**在那个包里并不存在**的名字上，配对本身是空的。改按「同包 + 测试名有交集」重数：`expect=green` **24** 条，有红搭档 **24** 条，没有搭档 **0** 条。⚠️ 这正是本节要说的那件事的活例：**一个数不被核，就会一直被引用。**

⚠️ **而正因为它便宜（不需要新证据），它容易被当成「进展」记错栏**：

    ✅ 「数出来的」  → 只进**「我们的检查有多严」**这一栏 —— 关于**我们自己**的事实
    ❌ 不得进       → **「柜台是怎么回事」**那一栏 —— 那一栏要**柜台的字节**

> ⚠️ **两栏都该涨，但它们不能互相顶替。**
> 否则明天会很好看，而 `rules_measured` 仍然是 2 ——
> **那时最容易发生的，是把好看的那一栏说成门槛松了一格。**

⚠️ **而「在动手之前」立下它是关键**（评审点出）：明天数完之后再立，
那时手上已经有一堆好看的数了 —— **而人不会用一条刚立的规矩，
去把自己刚拿到的成果划到次要那一栏。**

（评审 20260910 提出并给出判据；⚠️ 新规矩下第五条落库的归属记录。）

### ✅ 数完了（20260910 12:48）—— **全部进「我们的检查有多严」这一栏**

⚠️ **`rules_measured` 仍然是 2，本节一个字都不改动它。**

#### ① 守卫被破坏钉过的比例

    测试函数 374 个（23 个包目录）
    破坏清单点名 157 个不同测试
    ⇒ 42.0%

⚠️ 反过来读才是有用的那一半：**58% 的测试从没被任何一条破坏验证过** ——
它们哪天死了，不会有任何东西说话。这不是「要补到 100%」，
是**补哪些**的问题，见 ③。

#### ② 从没被破坏改过源码的生产包：24 个里有 6 个

    account            ⚠️ 核心包，10 个测试，零破坏
    types
    internal/decimalx
    refdata/live
    cmd/refdata-sync
    cmd/settlement     ⚠️ 227 行，零测试零破坏

⚠️ **第一遍我数错了方向**：按「哪个包的测试被跑」数，`margin`/`fee`/`pnl` 都显示零 ——
而它们其实是通过 `conformance/fixture` 的对拍被验的。
**破坏改的是一个包的源码，跑的是另一个包的测试**，两个口径必须分开数。
（`cmd/settlement` 那 227 行主要是 CLI 胶水，解析本体在 `refdata/exchange`，那里有 7 条破坏。）

#### ③ ⚠️ 破坏密度最高的是**对拍与探针**，最低的是**被对拍的那些纯函数**

    conformance/fixture   88 条        cmd/oracle/probe   31 条
    margin  2 条   fee  2 条   pnl  1 条   account  0 条

⚠️ 而 `margin`/`fee`/`pnl`/`account` 正是逐日盯市的**算术本体**。
⇒ 可执行的一句：**下一批破坏优先补这四个包**，而不是继续加对拍侧的。
⚠️ 理由不是「数字难看」：对拍侧的破坏靠夹具喂数据，
**夹具没覆盖到的路径，破坏也走不到** —— 纯函数包的破坏没有这个限制。

#### ④ 一条已经成立、而此前没人数过的

    expect=red 250 条，其中 want 为空 **0 条**

⇒ **每一条红破坏都验到了第三层**（红在被测性质上），不只是「红了」。
这是本次数出来的唯一一个「已经全对」的性质。

#### ⑤ 一个几乎没被用的能力

    带 also 的多文件破坏 **2 条**（0.7%）

⚠️ 而 `Also` 字段自己的注释写着它补的是「有些破坏**天然是多文件的**，
单文件工具表达不出来，于是那条守卫只能验一半」。
⇒ 2 条说明**那个洞多半还开着**，只是没人再去找它。

#### ⚠️ 一项**数不了**的，如实记

想数「有多少测试有反空转下界断言」。只能按散文关键词数：
68/99 个测试文件出现过「判别力 / 空转」字样，共 252 处。

> ⚠️ **那是「讲」不是「用」** —— 一个在注释里谈空转的测试，
> 与一个真的对空集合断言了下界的测试，这个数分不开。

⇒ **不入账。** 记在这里是为了让下一个想数它的人不必再走一遍。

#### ⚠️ 数的过程里撞出来的一件：破坏的编号前缀**不是全局唯一的**

    前缀 1  被 12 条共用      前缀 4  被 11 条共用
    前缀 2  被 12 条共用      前缀 5  被  9 条共用
    前缀 3  被 12 条共用      前缀 6  被  4 条共用
    前缀 7 及以后             各 1 条

⇒ **同一个字段里混着两套编号方案**：早期按**批次**编号（每批从 1 起），
后来改成**全局**编号。60 条落在前一套里，彼此撞号。

⚠️ 与今天那件事是同一个形状：`test` 字段同时装得下「前缀」和「函数名」，
**一个位置容纳两种含义，而读的人只会想到自己此刻要的那一种。**

**现状核过了**：仓库里目前**零处**歧义引用 ——
`silent-risks.md:3267` 那个「破坏 6」是误报（它是「干净 0 / 破坏 6」里的数）。
我今天引用过的 70 / 131 / 265 / 267 / 270–275 / 280 / 284 **每个都唯一**。

⇒ **动作（欠着）**：把前缀 1–6 那 60 条重编到全局序列，
并加一条守卫「破坏名的数字前缀全局唯一」——
与 `TestMethodologyNumbersAreContiguous` 同理由：**编号是引用地址**。
⚠️ 没有现在做：60 条重编号不是开盘前半小时该动的事，而它此刻**不咬人**（零处歧义引用）。

#### ✅ 那个动作当天就做了（12:48 → 12:59），重算一遍

    清单            274 → **302** 条（+28）
    被钉过的测试    157 → **180** 个 ⇒ 42.0% → **47.9%**
    零破坏的生产包    6 → **3** 个（account、internal/decimalx、types 出列）
    核心四包        account 0→5   margin 2→5   fee 2→6   pnl 1→5
    internal/decimalx  0 → **6**       types  0 → **6**
    want 为空的红破坏  0 → **0**（新加的 28 条每条都验到第三层）

⚠️ **剩下的 3 个零破坏包**：`refdata/live` / `cmd/refdata-sync` / `cmd/settlement`
—— 三个都是**取外部数据的通路**，破坏它们要么改网络层要么改解析层，
而解析层（`refdata/exchange`）已有 7 条。**它们不是同一类欠债**，
下一批之前先想清楚要验的是什么。

#### ⚠️ `types` 那一批揭出的两个洞，比那六条破坏本身重要

    ①  After / Before 在 types 自己这里**一条都没测过**
       把 After 改成 `d < o`：types 整包**全绿**，红的是 account 三条 ——
       而它们红在**症状**上（「下一交易日不晚于当前交易日」，结算被拒）
       ⚠️ 顺着那句话去查，人会先怀疑结算，不会怀疑 `>`
       ⚠️ 而 account.Settle 的守卫正是 `!nextDay.After(day)`：
          方向一反，「不能倒着结算」掉个头变成「只能倒着结算」

    ②  「三位年月恰好等距时拒绝定年代」—— **全库没有任何东西测它**
       关掉 `if tie` 之后 `go test ./...` 全绿。后果是郑商所三位合约码
       跨十年等距时**静默挑一个年代**：
          MA109 @ 20260910 → 候选 2021-09 与 2031-09 各距 60 个月
          挑一个 ⇒ **年份差十年、却完全正常的合约**

> ⚠️ **两个洞的共同点：它们不产生错误，产生的是一个看起来正常的值。**
> 而 ① 更隐蔽 —— 它**有**红，只是红在离病灶三层远的地方。
> 「有测试红了」与「红在能指向病灶的地方」是两件事。

⇒ 两条都已补测试（`TestTradingDayOrdering` 含相等那一格、
`TestDecadeTieIsRefused` 含判别力那一侧），各配了红搭档。

⚠️ `internal/decimalx` 那一批里 293 值得单记：`TestLibrarySemantics`
**直接调 `v.Round(Cents)` / `v.Truncate(Cents)`，从不经过 `Apply`** ——
它测的是**依赖库的语义**（它的注释写明了这一点），
所以它看不见「`HalfUpToCent` 派发错了」。一眼看去它像是盖住了取整。
实测下来 `TestThreeCandidatesDiffer` 抓得住 ⇒ **没有洞，而这件事此前没人核过。**

⚠️ 这一批里最值钱的不是那 16 条，是**它逼出来的两句缺掉的断言**：

    margin  TestBothTermsAlwaysSummed 只查了 r.Company，没查 r.Exchange
            —— 而它的名字承诺「两项永远相加」，且 CTP 对拍那一侧**已登记为盲区**
            （破坏 265：按手数那一项在那批样本上恒为零）⇒ **两边都盖不住**
    fee     破坏 4 在对拍侧登记为「平今与开仓同费率、分不开」，
            而 fee 自己的 TestThreeTiersAreDistinct **抓得住** ⇒ 那个盲区只在对拍这一层

> ⚠️ **「某一层盖不住」不等于「没人盖得住」** —— 而盲区登记在哪一层，
> 决定了去哪里找它的红搭档。破坏 265 找到了（对拍侧确实盖不住），
> 破坏 4 没找 —— 它的红搭档一直就在隔壁包里。

## ⚠️ 两个来源的**独立性属于哪一档**（20260910 补，先答了免得将来再推一遍）

    快期模拟   天勤**自己的模拟撮合与结算**，DIFF 协议
    SimNow     **CTP 柜台**，CTP 协议
    依据       [fidelity.md](./fidelity.md):267 —— **快期不是 CTP 柜台**

⇒ 两个组织、**两套独立写出来的实现**、两种协议 ⇒ 按 silent-risks.md 的三档判据，
**属于「中」档**：共同失效要求两套独立系统犯同一个错。

⚠️ **不是「强」档** —— 强档要求第二来源供的是**候选集**而非答案
（`kq_facts` 37 那样：上期所日行情并列三个互不相同的价，柜台从中选一个）。
两个柜台各自给出**答案**，那是中档。

⚠️ **也不是「弱」档。**我一度按「若两边都照同一份 CTP 规范实现」判成弱 ——
**而那个「若」我没有查，仓库里早就写着答案**（见 silent-risks.md 那一段）。

⇒ 后果：`kq_facts` 42 那半句将来拿到夹具时，**独立性这一项已经答完了**，
剩下要答的只有「证据在不在库里」。

## ✅ `rules_measured` 的升格判据（2026-09-09 使用者裁决，**已生效**）

⚠️ **这一节已生效**，计数已按它动过：`rules_measured` 1 → 2，并新增分母 `rules_listed`。

判据的候选措辞（评审方提出，实现方已独立复核过下面的数）：

> **第二个独立来源必须能「分开候选」，不能只是「对上数值」。**
> 若几个候选解释在该来源上给出**同一个数**，它对这条规则没有判别力 ——
> 它证的是**数据**，不是**规则**。

它的来历是同一天的一次自我更正：判断某文件换行是否错乱时，
「gofmt 差异正好是两倍」被当成了证据，而**一个真的格式错乱的文件同样是两倍**。
拿同一把尺子去量升格问题，两条现存事实的结论正好相反：

| | 判据下的结论 | 理由 |
|---|---|---|
| `kq_facts` 20（快期昨结算价 == 上期所今结算价）| **不该升格** | 只有一个候选。对不上意味着**数据**错了，不是规则是另一条 |
| `kq_facts` 37（`position_price` 的昨仓基线）| **该升格** | 上期所同一份日行情里**收盘 3177 与结算 3163 并列**，两个候选给出不同预测 |
| `kq_facts` 1（今仓保证金基准）| **不该升格** | ⚠️ CTP 拿到了判别性观测（三候选、单份夹具就分开），**但它给的值与快期不同**（开仓价 vs 昨结算价）。两个来源给出**不同的值**叫「一个参数被量到两个取值」，不叫「同一条规则被同向证实」。见 [probes.md](./probes.md) §6.8 |
| `kq_facts` 42（平仓挂单不冻保证金、但冻手续费）| ⚠️ **暂不提交裁决 —— 证据只有散文** | 20260910 CTP 侧实测：`FrozenMargin` 整整 0、`FrozenCommission` 非零，撤单完整释放。⚠️ 与前两条的分别在于**两个来源给出同一个值**（0），不是两个取值；且它**能分开候选**（若冻保证金，1 手会是 5000 上下）。⚠️ **裁决时要看到这句**：只有「冻不冻」这一半同向，**基准那一半两个口子不同**（见第 42 条），所以够格升格的是**那半句**，不是整条。⚠️ **20260910 复核后卡住**：那半句的证据（`FrozenMargin = 0`）当时**只出现在控制台上** —— `ctp-order` 直到那天才有 `-dump`，夹具里 0 处（silent-risks.md 77）。⚠️ **开仓**那一侧当天补上了夹具（`ctp-frozen-20260910.json`，`FrozenMargin 4808`），而**平仓**那一侧要账上有仓才拍得到 —— 已挂到重做昨仓实验的清单上。⇒ **有夹具之后再提交裁决**，不然升的是一条自己都翻不出来的结论 |
| `kq_facts` 3（手续费「按额 + 按手」相加）| **不该升格** | ⚠️ 理由与上一条**不同，而且更硬**：CTP 侧第一次真正分开了「相加」与「二选一」（`rb2701` 两项都非零，两个观测解出 a=万分之1、b=0.005），⚠️ **而快期在结构上给不了第二来源 —— 不是没测，是测不出来**。见下方专段 |

第 37 条的两个预测（昨 3 手 + 今 1 手 @3166，见 `position-frozen-20260909-5.json`）：

    用收盘价  (3×3177 + 1×3166)/4 = 3174.25   ← 柜台给的就是这个
    用结算价  (3×3163 + 1×3166)/4 = 3163.75

⚠️ **但升格它不会让 1/13 变成 2/13。** §13 的十四行里**没有**一条对应
「`position_price` 的昨仓基线用哪个价」—— 第 37 条不是解决了一条挂着的未知，
是回答了一个**从没被写进清单**的未知。正确记法是两笔一起记：

    rules_pending  +1   本该在册，此前漏了
    rules_measured +1   同一次实测把它解决了
                      → 2 / 14，而不是 2 / 13

> ⚠️ **只记解决、不记发现，分子涨而分母不动，比值会凭空变好看。**

~~⚠️ **为什么今晚不动**：`rules_measured` 是 tag 门槛之一，改判据等于改闸口宽度；
而**想放松它的动机恰好在它挡路时最强**，今晚它正挡着路且无人在旁。~~

**20260909 使用者当面裁决，四件一并定下：**

    a  这条措辞**采纳**
    b  第 37 条**升格**            rules_measured 1 → 2
    c  分母**新增** rules_listed   §13 一共问过几条，只增不减
    d  第 20 条备注写明**不够格**  它有第二来源，但那个来源分不开候选

⚠️ 这条澄清本身是**收紧**（同时否掉第 20 条、通过第 37 条），这正是它像原则而不像借口的地方。

#### 记账那一笔为什么要两个分母

第 37 条**从没被写进 §13** —— 它不是解决了一条挂着的未知，是回答了一个没人问过的未知。而 `rules_pending` 只数**还欠着的**（已收敛的行划掉、不在册）。于是两个方向会叠在一起：

    只记「解决」不记「发现」    分子涨、分母不动
    已收敛的行离开 pending      分子涨、分母**缩**

⚠️ **每一步单看都合理，叠起来比值凭空变好看。**
于是 §13 补上第 15 行（新增即收敛），并新增只增不减的 `rules_listed`：

    rules_listed = rules_pending + 已收敛     15 = 13 + 2
    rules_measured ≤ 已收敛                   2 ≤ 2

两条都由 `TestRulesListedMatchesTable` 钉住。⚠️ 第二条是**上界不是等式**：一条已收敛只说明它被测掉了，**不说明**它有第二个独立来源 —— 后者才是升格门槛。把它写成等式就等于把「够不够格」这个判断机械化掉，而那正是这一节要人来定的东西。

## `kq_facts`：关于快期模拟这个口子的已实测事实

⚠️ **这一节与 `rules_measured` 是两回事，分开记是刻意的。**

交易日 20260908 的夜盘量到下表这些，而 [cn-futures-rules.md](./cn-futures-rules.md) §13
那张待实测表只划掉了一条。原因不是量得不够，是两个问题不同：

> 这里记的是「**快期模拟怎么实现**」；§13 问的是「**规则是什么**」。
> 前者是后者的证据来源之一，不是后者的答案。

把它们记在同一个数里，就会出现「实测 6 条」这样的说法，
而读的人会以为规则定了 6 条。**一个把两种东西合并的计数，比没有计数更坏。**

| # | 事实 | 证据强度与边界 |
|---|---|---|
| 1 | **今仓**持仓保证金的基准价是**昨结算价** —— ⚠️ **只在快期这个口子上成立** | 四条失败方向不同的证据；⚠️ 昨仓未测。⚠️ **20260910：SimNow/CTP 上量出的是「开仓价」，不是昨结算价** —— 三个不同成交价（3155/3150/3146）各自给出**整整 16.0000%**，今结算价在持仓期间动过而占用保证金一次没动（[probes.md](./probes.md) §6.8）。⇒ **这不是谁量错了，是「保证金基准」本来就是柜台的一个参数**（CTP 显式列成 `MarginPriceType` 的多个取值），本库的 `margin.PriceBasis` 必须是入参而不是常数。⚠️ 本条**不因此进 `rules_measured`**：两个来源给出的是**不同的值**，那叫一个参数被量到了两个取值，不叫同一条规则被交叉证实 —— 把它算成升格会让那个计数变成「我量过几次」 |
| 2 | 保证金率**分品种**，至少三档（`rb`/`m` 7%，`i`/`cu` 11%，`ag` 22%） | 八合约两交易所；⚠️ 我先用三个同为 7% 的样本得出过相反结论，见 [silent-risks.md](./silent-risks.md) |
| 3 | 手续费**两种收法并存**：`m` 每手固定额 1.5 元，`rb` 按额万分之 0.1 | ⚠️ 已由日盘的成交截面**大幅加强**：`m` 三个月份费额恒为 1.5 而昨结 3404/3338/3019 各不相同；`rb` 三个月份费额 0.31/0.3158/0.3189 各不相同而费额÷昨结恒为 0.0001。两类各自都是**正面**证据，不是「一个对上一个对不上」 |
| 4 | 按额手续费的基准价是**昨结算价，不是成交价** —— ⚠️ **已知偏离真实 CTP** | ⚠️ 原证据是「同合约一买一卖差一个 tick」，只排除了成交价。日盘成交截面给出强得多的：`cu2701` **八个不同成交价、一个费额**；九个合约无一例外。且 `rb` 三个月份的**外推命中** —— 用 rb2701 标定的费率算出 rb2610 昨结 3100、rb2705 昨结 3189，与独立观测到的报价分毫不差 |
| 5 | **单向大边在此口子上未启用**（跨合约与同合约双向都是逐条相加） | 三次账户级采样 ×2 组样本；⚠️ 因此该口子**测不出**大边的合并范围 |
| 6 | `Balance` / `Available` / `RiskRatio` 三条恒等式在 float64 表示误差内成立（最大残差 `1e-10`） | ⚠️ 原记作「精确成立」，那是 4 位打印精度下的错觉。核对已落成测试，跑遍每一份夹具 |
| 7 | 挂单冻结：`FrozenMargin` / `FrozenCommission` 从 `Available` 扣、不进 `Balance`、不进 `Margin`、撤单完整释放 | 这一条**同时被 CTP 接口文档支持** —— ~~所以它是唯一进了 `rules_measured` 的~~⚠️ 「唯一」已过期：20260909 第 37 条升格后是**两条**。⚠️ 本条是**第一条**进 `rules_measured` 的 |
| 8 | 冻结保证金按**昨结算价**算，不按委托价（跌停挂单，冻结额等于该合约每手持仓保证金） | 单合约单方向；⚠️ 未覆盖卖开与其他品种 |
| 9 | 手续费**不按分取整**（`ag2702` 每手 `12.07125`，五位原样保留） | ⚠️ 只排除「到分」；更细的位数没被考验过 |
| 10 | `static_balance = pre_balance + deposit − withdraw` | ⚠️ 只有 2026-09-07 那两份**旧夹具**入金非零，这条靠它们才测得出；`− withdraw` 项仍未参与过运算 |
| 11 | CTP 的 `Algorithm`（盈亏算法）在此口子上是 `'1'` 浮盈浮亏都计算 | 浮盈 6 份 + 浮亏 6 份，结存恒等式两边都成立，排除其余三个取值 |
| 12 | **时段边界本身不动任何非行情字段**：今昨仓不滚、逐日盯市基线不重置、保证金不重算 | 跨 23:00–09:00 停盘、期间无成交；⚠️ 它的用处是**归因**——今晚结算后的变化因此可以归给结算 |
| 13 | 空仓方向上 `open_price` / `position_price` / `margin` 给的是字符串 `"-"`，**不是 0** | **188 份持仓截面，188/188 无反例**（⚠️ 188 是 **20260908** 的语料规模，不是当前的），是本表里唯一一条干净的全称命题；⚠️ 不许推广到成本字段，见第 15 条。⚠️ 20260909 补守卫 `TestEmptySideGivesDashNotZero`：**此前它只有一个冻结的计数** —— 而「本表里唯一一条干净的全称命题」这个说法本身，正是它最该有守卫的理由。新语料上出现反例不会有任何动静，文档里的 188/188 永远是 188/188。现跑全语料：空仓方向 2187 格全是 `"-"`、**有仓方向 609 格全是数值**（后一半缺了就与「这三个字段永远给 `"-"`」分不开）|
| 14 | ~~今昨拆分的成本与保证金**柜台不填**~~ ⚠️ **已被第 28 条推翻**（原文保留）：今昨拆分的成本与保证金**柜台不填**：`open_cost_*_today/_his`、`position_cost_*_today/_his`、`margin_*_today/_his` 恒为 0 | 188 份截面全为 0，**其中 47 个方向确有今仓**（`volume_*_today > 0`）——「今天没今仓所以是 0」被否掉了；⚠️ 昨仓侧今晚才测得到 |
| 15 | `"-"` 与 `0` 在**成本与盈亏**字段上是**路径依赖**的，不是当前状态的函数 | 同一个合约 `SHFE.rb2610`、同样两边空仓，一份截面 `float_profit` 给 `"-"`、另一份给 `0`；⚠️ 本库没有「这个字段今天被设过没有」这个状态，**复现不了** |
| 16 | 开仓均价确实是**加权平均**，不是取首笔/末笔/最新价 | 两个交易所各一次：`rb2701` 2 手 @3151 + 1 手 @3171 → 3157.666667；`m2701` 2 手 @3411 + 1 手 @3423 → 3415。⚠️ 判据落在**任何单笔成交价都取不到的值**上，所以不可能是碰巧撞上 |
| 17 | 按额费率按**品种**，基准是**各合约自己的昨结算价** | `rb` 三个月份，三个不同费额 ÷ 三个不同昨结 = 同一个数。⚠️ 这是一次**外推**：标定用一个月份，另外两个的昨结是**独立观测**的，不参与标定 |
| 18 | **平今费额恒等于开仓费额**，九个合约无一例外 | ⚠️ 这条的用处是**划出盲区**：平今费率这个维度在这个口子上**测不出来**（快期把它设成与开仓相同），而它正是 DIFF 的 `quotes.commission` 丢掉的那个维度（probes.md §4）—— 两处独立地指向同一个缺口 |
| 19 | 本交易日**一笔平昨都没有**（全部平仓都是 `CLOSETODAY`） | ⚠️ 记下来是因为它决定了当晚第 8 条实验的必要性：平昨费率**当时**零观测，不是「量到了等于平今」。⚠️ **已被第 34 条接续**（20260909 首次平昨）——「至今零观测」那句话到此为止；本行留档是因为它记的是**那一个交易日**的形态 |
| 40 | **平仓消耗顺序在这个口子上结构性地测不出来** | 两条腿各堵一半：`NoUseHistory`（大商所）持仓**从来没有昨仓** —— 419 个持仓截面里大商所占 190 个，有昨仓的 **0 个**；`UseHistory`（上期所）的 `CLOSETODAY`/`CLOSE` **已经指定了哪一边**（第 32 条）。柜台**从来不需要在今昨之间做选择**。⚠️ 与第 5 条（单向大边未启用）同形：结论是**要换口子**，不是再多跑几次实验；§13 第 4 条的状态因此从「未动，等昨仓」改成「测不出来」——**两者不是一回事**。⚠️ `TestReplayIsUnambiguous` 报「歧义 0」**不能**拿来加强本条：那个 0 的成因是重放里根本没有昨仓（夹具只含当日成交），与结构性理由无关 —— 拿它当证据等于把「样本缺失」读成「性质如此」 |
| 39 | **快期在 `DCE` 上不只是接受 `CLOSETODAY`，而是**常规成交**它** | 全部夹具里 `DCE/CLOSETODAY` 成交 **224 笔**（`SHFE/CLOSETODAY` 224 笔、`SHFE/CLOSE` ~~1 笔~~ **18 笔**、`DCE/CLOSE` **0 笔**）。⚠️ `SHFE/CLOSE` 那个数原写 1，那是写下本条那天的语料规模 —— 20260909 复核为 18，结论不受影响，更正是因为**一个没人复核的数会一直看起来像刚数过**。⚠️ 20260909 补两条守卫：`TestDCEFillsCloseTodayRoutinely` 钉**下界**（不是那个具体的数，数会随语料长），`TestDCEHasNeverUsedBareClose` 是 `DCE/CLOSE` 零观测的**绊线** —— 关于**缺席**的观测在新语料到来时会安静失效，没有绊线的话第一笔 `DCE/CLOSE` 不会有任何动静。⚠️ 这把第 30 条从「挂得住」加强到「成交得了、而且是常态」。⚠️ 后果落在**分层**上：`position.Close` 因此**不能**对 `NoUseHistory` 拒绝 `CLOSETODAY`，否则夹具里那 224 笔一律重放不了 —— 报单校验属于 `order`（v0.4.0），状态机这一层照收。⚠️ 本库仍按 CTP 建模，规则不改 |
| 38 | **平仓按「持仓均价」冲减成本，`position_price` 因此一动不动** | 20260909 平昨 1 手前后：4 手 @3174.25（`position_cost=126970`）→ 3 手 @**3174.25**（`position_cost=95227.5 = 3174.25×3×10`）。⚠️ 若平仓时重算基线会得到 (2×3177+1×3166)/3 = 3173.3333…，差 0.9166… —— **四位打印精度下看得见**，但在只有一手的样本上会消失。⚠️ 这条是从一次「对不上」里挖出来的：跨来源核对那条测试拿组合式去比平过仓的截面，差值不是噪声。本库的 `position` 若用别的价冲减成本，权益曲线会整条错开，而每一步的数看起来都正常 |
| 37 | **柜台的逐日盯市基线组合式被精确刻画，且收盘价这一端有了独立来源** | `position_price = (昨仓×收盘价 + 今仓×开仓价) ÷ 总手数`。实测 `rb2701` 今1/昨3：(3×3177 + 1×3166)/4 = **3174.25**，与柜台分毫不差；`position_cost = 126970` 同样对上。⚠️ 其中 **3177 由上期所日行情独立确认**（`CLOSEPRICE=3177`、`SETTLEMENTPRICE=3163`、`PRESETTLEMENTPRICE=3158`，三个数互不相同故样本有判别力）—— 这是本表**第二条有独立来源**的核对（第一条是第 20 条）。⚠️ **20260909 使用者裁决：本条升格进 `rules_measured`**（1 → 2）—— 够格的理由不是「对上了」，是那三个价**互不相同**，两个候选给出不同预测（用收盘 3174.25 / 用结算 3163.75），第二来源因此**能分开候选**。⚠️ 同一把尺子把第 20 条挡在外面。⚠️ 本条此前**不在 §13 清单里**，升格时一并补进去（第 15 行，新增即收敛），否则分子涨而分母不动。此前「3177 是收盘价」只有柜台自己的数据支撑，而拿柜台的数解释柜台自己的基线是同义反复。⚠️ `ParseSHFE` 一直把 `CLOSEPRICE` 丢掉，现在接上了。⚠️ **本库不改**：中国期货的资金结算走**结算价**（cn-futures-rules.md §5），快期用收盘价是这个口子的实现 —— 这条已知差异现在两端各有独立出处 |
| 36 | **两条盈亏基线第一次被同一个截面干净分开** | 20260909 夜盘 `SHFE.rb2701` 多昨 3 手：`last=3168`、`position_price=3177`、`open_price=3157.6666666666665`；柜台报 `position_profit=-270` 与 `float_profit=+310`。逐个核对：(3168−3177)×10×3 = −270、(3168−3157.6667)×10×3 = +310。**同一批持仓、同一个最新价，两条基线给出反号的两个数** —— `Lot` 同时带 `Basis` 与 `OpenPrice` 这个设计到此为止是被实测支撑的 |
| 35 | **`position_price` 不跟最新价走**：行情滚过之后它仍是 3177 | ⚠️ 这**证伪了第 26 条剩下两种读法中的一种**。21:00 行情滚到新交易日（`datetime=2026-09-08 21:00:58`、`last=3168`、`pre_settlement=3163`），而 `position_price` 一动不动还是 3177。所以它不是「跟着最新价」，是一条**定住的基线**。⚠️ 但「3177 是收盘价」这个标签目前**只有柜台自己的数据支撑**：上期所日行情本库只留了结算价，没留收盘价（`ParseSHFE` 丢掉了），所以还缺一个独立来源 |
| 34 | **平昨手续费额 == 开仓额**，于是平昨档在本口子上**测不出来** | 20260909 首次平昨（此前一笔都没有，见第 19 条）：`rb2701` 平昨 1 手，成交记录 `offset=CLOSE`、`commission=0.3163`，与同交易日同合约的两笔开仓**分毫不差**。⚠️ 这条与第 18 条同形：它的用处是**划出盲区**，不是「量到了等于开仓」。CTP 六个费率里平昨那两档在这个口子上和平今一样量不出来 |
| 33 | **`position_cost_*` 的今昨拆分是**结算时**写的，盘中不更新** | ⚠️ 这条**细化并部分推翻**了第 28 条的读法。20260909 夜盘 `rb2701` 开出多今 1 手、空今 1 手之后：`position_cost_long_his=95310`（= 3×3177×10，昨仓那部分）、而 `position_cost_long_today` 与 `position_cost_short_today` 都给 `"-"` —— **有量却没值**。写哪一侧由 `PositionDateType` 定：`UseHistory` 结算时全变昨仓故写 `_his`，`NoUseHistory` 仍是今仓故写 `_today`（`m2701` 的 `position_cost_long_today=102450`=3×3415×10 就是这么来的）。⚠️ **后果**：有盘中成交时 `_today + _his ≠ position_cost`。实测合计 126970 = 95310 + 31660（今 1 手 × **开仓价** 3166 × 10）—— 拿拆分去凑合计会差一整块，且不报错 |
| 32 | **`UseHistory` 合约上 `CLOSE` 就是「平昨」，不是「由柜台挑一边」** | ⚠️ 这条**推翻了第 25 条的解读**，而更该记的是：**本仓库自己的文档一直是对的**。[cn-futures-rules.md](./cn-futures-rules.md)「今仓与昨仓」一节 2026-09-07 就从拒因原话记下了「`CLOSE` 在此类合约上被解释为平昨，不是由柜台择优」。第 25 条那句「由它自己决定消耗哪一边」是一次**没有依据的外推**（那次样本里只有昨仓，根本没有可挑的），而它与本仓库的规则文档**直接矛盾却没人发现** —— ⚠️ 两处各记一份同一件事，矛盾时不会有任何动静，这本身是个教训。20260909 在多今 1/多昨 3 的 `rb2701` 上：`CLOSETODAY` 冻 `volume_long_frozen_today`、`CLOSE` 冻 `volume_long_frozen_his`；而在只有今仓的空头方向上发 `CLOSE` 被**拒单**，柜台原话「平昨手数超过昨仓持仓量」。⚠️ 也就是说 `UseHistory` 上**没有消耗顺序这回事**，实验 4 要问的那个问题只在 `NoUseHistory` 上才成立 |
| 31 | **持仓侧的挂单冻结按今昨拆分，而且拆分字段**真的被填**；撤单逐字段完整释放** | 20260909 收盘后两次实测：`DCE.m2701`（今 3 / 昨 0）挂 1 手平仓 → `volume_long_frozen=1` **且** `volume_long_frozen_today=1`；`SHFE.rb2701`（今 0 / 昨 3）→ `volume_long_frozen=1` **且** `volume_long_frozen_his=1`。撤单后六个字段逐字段回到挂单前。⚠️ 与第 28 条**不矛盾但必须分开读**：成本与保证金的今昨拆分柜台大多不填，冻结的拆分**填**。✅ **20260910：六个冻结字段全部拿到非零观测** —— 最后一个 `volume_short_frozen_his` 靠结算后那手**过夜空仓**补上（short 发 CLOSE 1 手 → 0→1，撤单完整释放）。⚠️ ~~**盲区**：两个样本里都只有一侧有量，「柜台按今昨拆」与「镜像到有量的那一侧」分不开~~ —— **当晚就解掉了**：在多今 1/多昨 3 的 `rb2701` 上，`CLOSETODAY` 冻 `_today`、`CLOSE` 冻 `_his`，柜台确实**按今昨拆**。见第 32 条 |
| 30 | **快期模拟接受 `DCE` 合约的 `CLOSETODAY`** | `DCE.m2701` 有今仓 3 手时发 `CLOSETODAY`，被接受并挂住，冻结结果与 `CLOSE` **逐字段相同**。⚠️ 这**不推翻** CTP 的 `NoUseHistory` 规则本身（本库照它建模），推翻的是 `cmd/oracle/kq/order.go` 里那半句「用错会被柜台拒单」—— 那是一个**关于口子**的断言，而这个口子不拒。⚠️ 与第 25 条成对：`UseHistory` 合约收裸 `CLOSE`、`NoUseHistory` 合约收 `CLOSETODAY`，**这个口子在开平标志上两个方向都比规则宽松**，因此它测不出 `PositionDateType`——20260909 那条结论靠的是**今昨仓滚不滚**，不是靠拒单 |
| 44 | **拒绝优先级：最小变动价位 > 涨跌停 > 可平量** | 20260909 快期模拟，SHFE.ag2702 与 DCE.i2701 各一遍。⚠️ 与 cn-futures-rules.md §9 的文档表**一致** ——而我曾据同一晚更早的一次实测得出**相反**的结论并真的把 `order` 的两项对调过。翻案的原因不是又量了一次，是发现**对照组根本不成立**：那一笔构造「偏离整数倍」用的零头是 0.7 个 tick，而柜台不把它当偏离（见第 45 条）。⚠️ 归因不认关键字，判据是两组 `last_msg` 逐字不等；守卫 `TestPriceTickBeforePriceLimit`、`TestRejectPriorityFromFixtures`。⚠️ 20260909 稍晚补上第四对：**合约（不存在）排在价格类校验之前** ——不存在的合约上的偏离整数倍价，回的是 311（合约不存在）而不是 412（整数倍）。⚠️ 边界：判出先后的只有几对，见本页 `reject_priority_measured`；真实 CTP 未裁决，见 simnow_pending#11 |
| 45 | **判据写死：`price mod tick ∈ (0, tick/2)` 才判「不是整数倍」**（`= tick/2` 落在**接受**侧，1/2 那一档已实扫过）—— 即柜台只在价格零头 < 半个 tick 时才说「不是整数倍」，零头 ≥ 半个 tick 的价格照单全收** | 20260909 同一时刻把零头扫成 1/10、1/3、1/2、7/10、9/10：前两个被拒，后三个**被接受**，价内的直接挂上去、委托记录里还留着那个带零头的价（如 `limit_price=13009.9`）。SHFE.ag2702（tick=1）与 DCE.i2701（tick=0.5）各一遍，边界都落在半个 tick 上 ——⚠️ **「零头」必须按向下取整的小数部分算，不是「到最近 tick 的距离」**。⚠️ **20260909 09:02 日盘内把边界钉死了**：此前那批系统性扫描全在盘后，盘中只有 1/3 与 0.7 两个点（分居两侧，但中间空着 —— 落在 (0.34, 0.69) 里的任何边界都解释得了）。日盘补扫 0.4 / 0.45 / 0.5 / 0.55 / 0.6：**0.4 与 0.45 报「不是整数倍」，0.5 / 0.55 / 0.6 报「涨停板」** —— 边界**恰好**在 0.5 且 0.5 本身落在接受侧，与盘后完全一致。守卫因此从「那段空着」的绊线换成**双向**断言 `TestCounterHalfTickBoundaryHoldsInSession`：盘中每一笔拿到那两句原话之一的委托，柜台的答案与本库 `offTick` 逐笔一致（48 笔）：后一种读法对「tick 对齐」反而更自然，而按它算 19524.7 的零头是 0.3 < 0.5，会得出与结论相反的假发现（评审方 20260909 第一遍就这么算的）。**随 tick 缩放**，不是某个绝对数。⚠️ 这条几乎肯定**偏离真实 CTP**（交易所侧任何非整数倍都要拒），列为已知偏离。⚠️ 它的第二个用处是**划出一个陷阱**：用零头 ≥ 半 tick 的价格去构造「不是整数倍」这一违规，构造出来的单子**柜台根本不认为违规** —— 第 44 条那次翻案就栽在这里。守卫 `TestCounterTickToleranceIsHalfATick`。⚠️ **还有一条边界：系统性的那次扫描是在收盘后跑的**（02:37 与 02:41，ag 夜盘 02:30 收、i 夜盘 23:00 收）。盘中只有一个点与这个模型相符（02:0x 的 20489.7，零头 0.7，回的是涨跌停而不是整数倍）。⚠️ 第 48 条说柜台根本不查时段，所以「盘中盘后走同一条校验路径」很可能成立 ——**但那是推论，不是观测**。下一个交易时段内要重跑一遍 |
| 46 | **开仓挂单冻结的保证金按昨结算价算，不是按报单价** | 20260909 两次独立实测：`SHFE.ag2702` 昨结 16262 × 乘数 15 × 22% = **53664.6**，`DCE.i2701` 昨结 740 × 乘数 100 × 11% = **8140**，与账户 `frozen_margin` 分毫不差。⚠️ 两笔的**报单价**分别是 13009.9 与 673.95，都远低于昨结算价 —— 按报单价会算出 42932.67 与 7413.45，**这不是一次同值的巧合**。冻结手续费同样按昨结算价、开仓档（12.1965 与 7.4）。⚠️ 它与持仓保证金同基准（第 1 条），但那是两件事：一个是挂单占用、一个是持仓占用，**同基准是量出来的，不是推出来的**。这条把 `frozenTotals` 里原本「没见过的情形不猜」那一支填上了 |
| 47 | **柜台有第二条回话通道 `notify`，带**数值码**；而一整类拒绝**只走**它** | 20260909：报单到一个不存在的合约（`SHFE.ag9912`）上，柜台在**委托记录里一个字都不写**（`status` 与 `last_msg` 全空），只从 notify 回 `code=311`；同一批里合法合约的偏离整数倍价回 `code=412`，且 412 的文案与委托记录的 `last_msg` 一致。⚠️ 后果：**本仓库此前所有的拒因实验都只读委托记录** —— 这类拒绝在那些实验里长得像「柜台没反应」，而那与「单子还挂着」一模一样。⚠️ 这些码是**天勤自己的**，不是 CTP 的 `ErrorID`，填不了 `ctperr`（simnow_pending#5）。⚠️ 落盘只收 `type`/`level`/`code`，**刻意不收 content**（仓库公开、泄漏不可逆）。⚠️ 原先写的理由是「白名单靠逐个字段点名，而自由文本不在保护范围内」——**那条按字面同样会否掉 `last_msg`**，而它也是自由文本且已在库（拒因实验的全部产出）。真正起作用的是两层：一是 `last_msg` 的词表被**我们下的单**界定（实测 4 种，全是拒因），而 notify 通道**设计上**能承载与我们的动作无关的东西 —— ⚠️ 但至今**没有观测到**一条非我们招致的 notify，所以这是通道性质不是实测；二是 `last_msg` 的**取值**有守卫（`TestLastMsgValuesAreKnown`），content 没有。⚠️ 原列了三个选项（不收 / 全收 / 把白名单下推到取值）。**20260909 使用者裁了第四条：文案要留，但只落本地、不进 git。**于是拆成两份，主键同为文件名：主夹具存 `code`/`level`/`type`（**进 git**，对拍要用），`testdata/probes/local/<名>.notify.json` 再带上 `content`（**不进 git**，`.gitignore` 挡）。⚠️ 为什么不是「整份夹具都只落本地」：那 95 份是**公开的**证据语料，整套离线对拍靠它们跑，改成本地件会让任何一个新克隆**测不了任何东西**。⚠️ 旁档**不被任何测试依赖** —— 一份测试依赖它的本地件，等于把测试变成只有一台机器能跑。守卫三条：`TestNotifyCodesAreKnown`（码要认得出）、`TestLocalOnlyEvidenceIsGitIgnored`（问 git 自己，不重实现匹配规则）、`TestCommittedFixturesCarryNoNotifyContent`（堵**另一条路**：有人给 Notify 加回 Content 字段时，前一条一个字都不会说）|
| 48 | **柜台根本不校验交易时段** —— 非交易时段的报单照收 | 20260909 从**已有语料**横着读出来（没有新实验）：313 笔从未被拒过的委托里，**217 笔的下单时刻落在该品种的任何一个时段之外** —— 16:09、17:57、01:07、`DCE.i2701` 更是在夜盘（21:00–23:00）收盘后将近四小时的 02:56:44 被收下。⚠️ 这条说的不是「本库还没实现时段校验」（那是 `order.CheckSession` 落在 `Unchecked` 里的原因），而是**就算实现了，这个口子也验不了它**：查时段的实现与不查的实现在这里对拍结果相同。⚠️ 「还没做」与「这个口子测不了」在清单上长得一样，处理方式相反 ——前者排期，后者要换口子（与第 5、40 条同形）。守卫 `TestOrdersAcceptedOutsideSession`。⚠️ 假设：`insert_date_time` 按东八区解释；它错了的话每一笔的判定都跟着错 |
| 49 | **委托记录里的 `frozen_margin` 只出现在「进了簿且尚未成交」的开仓委托上** | 20260909，431 条开仓 + 404 条平仓委托：进簿未成交 **60/60 有**、被拒 **339/339 无**、已成交 **32/32 无**、平仓 **404/404 无**。一致的解释：进簿时出现，成交时被清掉（冻结转成持仓保证金），撤单后**留下最后那个值**。⚠️ 它更正了第 42 条里那句「柜台的委托记录里根本没有这个字段」——**那是从三份平仓样本推出来的**，而从一类样本上的缺席推不出字段不存在。⚠️ 陷阱：已撤掉的委托上它仍是非零的历史值，是**戳记**不是当前占用；直接把委托记录里的 `frozen_margin` 加起来会偏大，而那个和看起来完全合理。守卫 `TestOrderFrozenMarginShape` |
| 50 | **`position_cost_*` 四格里的 `"-"` 跟的是**今昨侧**，不是「有没有仓」；而**未经结算**时四格全是数值 `0`、连 `"-"` 都不出现** | 20260909 横读全语料**466 条持仓记录，无反例**：未结算态 372 条（四格全 0）、已结算 94 条（按 `PositionDateType` 挑一侧写，**另一侧两个方向都给 `"-"`**：`UseHistory` 的 `rb2701` 写 `_his`、`_today` 两格是 `"-"`；`NoUseHistory` 的 `m2701` 反过来）。⚠️ 判别样本 48 格：`rb2701` 结算后那几份里**空头有今仓 1 手**而 `position_cost_short_today` 是 `"-"`，同一条记录 `position_cost_short_his` 无仓却是数值 `0`。⚠️ 它**更正了第 28 条的措辞**（「空的那一侧」），也**在第 15 条上挖了个例外**：这四个字段上 `"-"`/`0` 不是路径依赖的，是 `PositionDateType` 与「是否经历过结算」的函数。守卫 `TestCostSplitAbsentSideFollowsPositionDateType` |
| 51 | **空头昨仓在全语料里从未存在过**，于是第 28/33 条的**全部非零证据都在多头侧** | 20260909 横读 466 条持仓记录：`volume_short_his` / `volume_short_yd` / `pos_short_his` / `volume_short_frozen_his` **>0 的次数各为 0**（四个字段都**在场**，不是缺字段），而空头**今**仓有 27 条。⚠️ 后果：「拆分规则是方向中性的」与「柜台只在多头侧写这个拆分」在当前语料上**给出完全相同的预测**，而本库建的是前者 —— 这不是一个待补的字段，是那一格**还没有判别力**。⚠️ 要分开只有一条路：一手空仓**活过结算**（今仓空头不过夜完全无效）。~~绊线 `TestShortHistoryCostHasNeverBeenObserved`，它会在采样成功那天**变红**~~ ✅ **20260909 16:20 那次采样成功了，绊线如期变红**：空头昨仓第一次出现（`volume_short_his=1`）。判定见第 52 条 —— **假说 A 成立，B 被否**。绊线已删（不是加例外），`protectedLegs` 里那条保护也随之划掉：种子已经用掉了 |
| 52 | **今昨拆分的写法是**方向中性**的 —— 柜台在空头侧照写不误** | ⚠️ 这条**判掉了第 51 条挂着的那个二选一**，而它等了一整天：空头昨仓在此之前 466 条持仓记录里出现 **0 次**，于是「规则方向中性」与「柜台只在多头侧写这个拆分」给出完全相同的预测。20260909 日终结算后（交易日滚到 20260910）`SHFE.rb2701` 空今 1 变成**空昨 1**，两个假说第一次分开：`position_cost_short_his = 31690 = 1 手 × 3169 × 10`，而多头侧 `position_cost_long_his = 95070 = 3 手 × 3169 × 10` —— **同一个每手基线 3169，两侧都写了**。⇒ **A 成立（本库建的就是这个），B 被否**。⚠️ 保证金同样方向中性：多 6642.3÷3 = 空 2214.1÷1 = 2214.1。⚠️ `_today` 两侧仍然都给 `"-"`，与第 50 条一致（`UseHistory` 合约写 `_his` 侧）。⚠️ **这份样本分不开的一件事要写出来**：`position_price = 3169` 而 `last_price` 也是 3169，因为采集在 16:20 收盘后、最新价此刻就是当日收盘价 —— 于是「基线是收盘价」与「基线是最新价」在这一份上同值，**本条不重复第 35 条的结论，也不当新证据**。守卫 `TestCostSplitAbsentSideFollowsPositionDateType` 现在有了空头侧的判别样本 |
| 43 | **可成交的限价单成交在对手价上，不在自己的报价上** | 20260909：`BUY/OPEN` 报 3321（涨停价）成交在 **3166**、`SELL/OPEN` 报 3004（跌停价）成交在 **3165**，同一时刻 `last_price=3165`。⚠️ 这条的用处是**划出一个前提**：要建模成交价就得知道盘口，而 `quoteKeep` **刻意不留** bid/ask（理由见那段注释：盘口每拍都动，会让同名夹具永远「内容不同」，冲突守卫天天报警）。⚠️ 于是 `match` 的成交价逻辑在当前证据上**不可验证** —— 写成什么样都不会红。这是个要人来裁的取舍，见 roadmap 的变更记录 |
| 42 | **平仓挂单不冻保证金、但冻手续费** | 20260909 三份挂着平仓单时拍的截面：账户 `frozen_margin` **恒为 0**，而 `frozen_commission` = 一笔的费额 0.3163。⚠️ 这补上了 `order.FreezeOf` 文档里标着「本库没有观测」的那处边界 —— 原来是**按 CTP 模型**实现的（持仓的保证金本来就占着），现在有实测支撑。⚠️ 「按模型实现」与「有实测支撑」是两种不同的可信度，而它们在代码上长得一模一样，差别只在注释里。⚠️ 顺带那句「柜台的委托记录里根本没有 `frozen_margin` 字段」**已被第 49 条更正**：那是从三份**平仓**样本推出来的。✅ **20260910：CTP 侧拿到第二来源，同向** —— 账上有多头今仓时挂卖平今 @涨停 3322，`FrozenMargin` **整整 0**、`FrozenCommission` 3.3270，撤单完整释放（probes.md §6.11）。⚠️ 它**能分开候选**：若平仓挂单也冻保证金，1 手会冻 5000 上下。⚠️ **但只有「冻不冻」这一半同向** —— 冻结手续费的**基准**两个口子不同：CTP 按**委托价**（3.3270 = 3322×10×万分之1+0.005），快期**不按委托价**（`limit_price` 3321 与 3004 两份样本给出**同一个** 0.3163 = 昨结 3163 基准）。⇒ `order.FreezeOf` 里那个基准必须是入参。⚠️ **把两半并成一句「已被两个来源证实」会把后一半悄悄带过去。** |
| 41 | **换挡是分条消息推的，中间有个不到两秒的窗口，截面在那一瞬**自己内部不自洽** | 20260909 抓到那一瞬：`watch-02`（`quotes.datetime=18:39:50.268`）的 `pre_settlement` 已经是 **3163**，而同一份截面里 `margin_long=6631.8` 反解出的基线是 **3158**；1.3 秒后（`watch-03`，18:39:51.500）两者都到位。⚠️ 这比第 29 条更细：不只是「账户侧 vs 行情侧」两大块不同步，**同一个合约的行情昨结算价与持仓保证金也分两条消息推**。⚠️ 用处是判据：一份自己就不自洽的截面，拿去与本库比毫无意义 —— 比出来的差异指向采样时刻，不是两边的口径。反解基线与它自己报的昨结算价比，**一处都不引用本库的计算**，所以不是循环论证。守卫见 `impliedMarginBasis` |
| 29 | **账户侧与行情侧不同步**：结算后 `trading_day`/`pre_balance` 已滚到 20260909，而 `quotes.datetime` 仍是 `2026-09-08 14:59:59.999500` | ⚠️ 后果可算：柜台的 `margin_long = 6631.8`，而 `6631.8 ÷ (10×3×0.07) = 3158` —— 那是**旧的**昨结算价。本库用 3163 得到 6642.3。这不是规则差异，是**时序**。⚠️ 它同时给第 26 条提供了**第二种解释**：`position_price = 3177` 可能不是「基线是收盘价」，而是「行情没滚、柜台还没重定基线」。两者 21:00 一开盘就分得开 |
| 28 | **今昨拆分只有 `position_cost_*` 会被填，且只在**经历过结算之后**；`open_cost_*` 与 `margin_*` 的拆分至今从未是真实数字** | ⚠️ 这条**推翻了自己的上一版**（原第 14 条「今昨拆分一律填 0」，依据 20260908 的 188 份截面）。20260909 结算后：`rb2701` `position_cost_long_his=95310`、`m2701` `position_cost_long_today=102450`；而空的那一侧给 `"-"`。一致的解释：这个拆分在**日终结算时**算出来。⚠️ **本条里「而空的那一侧给 `"-"`」这句判据是错的，已被第 50 条更正**：`"-"` 跟的是**今昨侧**，不是「有没有仓」——同一条记录上空头**有今仓 1 手**而 `position_cost_short_today` 照样是 `"-"`。两个判据在写下这条时的语料上给出完全相同的预测 |
| 27 | **`volume_*_his` 与 `volume_*_yd` 的差别测出来了** | ⚠️ 结算后 `DCE.m2701`：`volume_long_today=3 / his=0`，而 `volume_long_yd=3 / pos_long_his=3`。**同一批持仓，两套口径给出相反的答案**：`_his` 是「按平今平昨规则算的昨仓」，`_yd` 与 `pos_*_his` 是「实际上是前一交易日开的」。⚠️ 这直接答了 §13 第 7 条，且本库的 `Lot` 一直同时带着 `Settled` 与 `OpenDay` —— 只是此前没有样本要求分开 |
| 26 | **`position_price` 结算后等于当日**收盘价**（3177），不是结算价（3163）** | ⚠️ 四份截面一致；两者差 14，这个样本把「结算价」与「收盘价」干净地分开了。⚠️ 剩两种读法（定死在收盘价上的基线 / 只是跟着最新价），今晚开盘即可分开。⚠️ `open_price` 穿过结算**不变**（3157.6666666666665）—— `Lot` 同时带两条基线这个设计第一次被实测确认 |
| 25 | **`UseHistory` 合约上柜台**接受**裸 `CLOSE`** | 用挂不上的限价发出、被接受、随即撤单。⚠️ 与 roadmap v0.4.0 那条「裸 CLOSE 必须报错」相左 —— 不推翻它（本库按 CTP 建模，simnow_pending#1 未裁决），但把「真实柜台会怎样」从猜测变成一个口子上的实测 |
| 24 | **`PositionDateType`：上期所滚今昨仓，大商所不滚** | ⚠️ 结算后 `SHFE.rb2701` 多今0/多昨3，而 `DCE.m2701` 多今3/多昨0；账户层结算**已完成**（`pre_balance` 推进、`close_profit` 归零），所以「大商所还没结算」被否掉。快期把 `NoUseHistory` 实现成「持仓永远记在今仓上」。⚠️ 同批实测：在只有昨仓的方向上发平今被拒，柜台原话「平今手数超过今仓持仓量」 |
| 23 | **`Calendar` 比柜台保守**：时段之外它拒答，而柜台任何时刻都报得出 `trading_day` | 实测：夹具 `status-20260908-6` 采于 **13:28:42**（午休 11:30–13:30 内），柜台照样报 `trading_day=20260908`。⚠️ 不是缺陷，是两者回答的不是同一个问题：「柜台当前处在哪个交易日」与「某个时刻属于哪个交易日」在盘中重合、休市时分岔 |
| 22 | **涨跌停价 = 昨结算价 × (1±比例)，再对齐最小变动价位；而两家交易所的取整方向不同** | ⚠️ 上期所 `cu` 9% / `rb` 5% 只命中**向下取整**，大商所 `i` 9% / `m` 6% 只命中**四舍五入**（遍历 0.5%–30% × 三种取整，各只有一组同时命中涨停与跌停）。⚠️ `ag` 20% 与 `rb2610` 5% **整除**，三种取整同值 —— 那两个样本对取整方向没有判别力，单独计数 |
| 21 | **「今结算价出现」与「账户结算发生」是两件事，时间上分开** | ⚠️ 实测：15:00:02.368 `settlement` 由 `"-"` 变成 3163（收盘后 2.4 秒），而 15:04 的截面里 `trading_day` 未变、`pre_balance` 未推进、**今仓仍是今仓**。把前者当成后者，今晚第 0 步的三态判定会直接走错分支 |
| 20 | 快期报的昨结算价与**上期所日行情**的今结算价一致 | ⚠️ ~~本表里**唯一一条有第二个独立来源**的~~ —— **「唯一」已过期**：第 37 条（20260909）是第二条，它自己写着「第一条是第 20 条」，而本条的「唯一」**没有跟着改** ⚠️ 两条互相矛盾地并排挂着，没有任何东西会因此变红。现表述：本条是**第一条**有第二个独立来源的。⚠️ 而「有第二个来源」与「那个来源够格升格 `rules_measured`」是两回事 ——按上方待裁决那条判据，本条恰恰**不够格**（只有一个候选，对不上说明数据错了，不说明规则是另一条）。证据：`rb2610/2701/2705` `cu2701` `ag2702` 五个合约、三个品种，上期所 `kx20260907.dat` 与快期分毫不差。它去掉了第 17 条外推里的一处循环 —— 此前费额与昨结算价**都来自快期** |

⚠️ **第 14 条已被第 28 条推翻，划掉但保留原文，且不计入 `kq_facts`。**

保留的理由与 §13 第 9 条同：一条被推翻的事实，
它**当时的证据**与**推翻它的证据**放在一起，才说得清「为什么当初那么想」。
删掉它，下一个人只会看到结论，看不到那条结论是怎么被修正的。

⚠️ 而它是被**自己的守卫**推翻的：那条测试当初写着
「哪天柜台开始填这些字段了，会有动静，而不是被当成一直如此」——
20260909 的结算一发生它就红了。**一条断言口子行为的测试，
它的价值不在绿的时候，在它红的那一天。**

⚠️ 第 16–19 条来自**成交截面第一次进夹具**之后的日盘批（自然日 2026-09-08 13:2x–13:4x）。
在此之前夹具只存持仓截面，也就是只存了均价 —— 而第 16 条问的正是「均价是怎么来的」，
第 17/18/19 条问的是逐笔的费额。**这些问题此前不是没答案，是问不出来。**

⚠️ 第 13/14/15 条是**读夹具读出来的**，不是新做实验得来的：
同一批 188 份截面此前只被逐条看过，没被**横着**统计过。
一个「量了就算用过了」的默认，会让已经付过代价的数据白放在那里。

⚠️ 第 13 条与第 15 条合起来才是完整的：`"-"` **在均价与保证金上是状态的函数，
在成本与盈亏上不是**。只记住前半句，就会把成本字段的 `"-"` 也当成「空仓」的同义词，
而那会在「当天平过仓」的合约上错。

⚠️ 第 4 条是本批唯一一条**已知与真实柜台不同**的，而它的偏差在同一天内是常数，
因此对拍时最容易被读成「对上了」。它必须进对拍的**已知差异表**，不能当成通过。

⚠️ **在册事实里进了 `rules_measured` 的只有第 7 条与第 37 条，这条线是刻意画的：**
第 7 条有**两个独立来源同向** —— 快期实测，加上 CTP 接口文档里同一条公式。

其余里只有第 20 条有第二个来源，而它证的是「某几个合约在某一天的结算价是多少」，
不是一条规则；**余下的只有快期一个来源**，而本批恰恰量到快期在手续费基准价上
偏离 CTP，所以「快期这样」不能直接读成「规则如此」。

**判据是「有没有第二个独立来源」，不是「量得准不准」。**

> ⚠️ 这一段原先写着「二十八条在册事实」「其余十九条」「余下二十六条」。
> 20260909 复核时那三个数**互相都对不上**（28−1=27，而下一句说十九，
> 再下一句说二十六），且 `kq_facts` 早已不是 28 —— 一段自己跟自己矛盾的计数
> 在这里挂了不知道多久，**没有任何东西会因为它而变红**。
>
> 于是按本页顶上那条规矩办：**不写数**，写结构，数去看上面的 `kq_facts` 表。
> ⚠️ ~~刻意**不**给 `rules_measured` 补一条「从表里数出来」的守卫 ——
> 它的升格判据本身正挂在上面的待裁决里，先把判据定了才谈得上机械化。~~
> **20260909 判据已裁决**，于是补了守卫 —— 但补的是**上界**不是等式：
> `rules_measured ≤ 已收敛`。理由没变：一条已收敛不说明它有第二个独立来源，
> 把它写成等式就等于把「够不够格」这个判断机械化掉，而那一步仍然要人来做。

## `doc_sections`：各文档的小节数

⚠️ **这张表存在的理由是一次静默的内容丢失。**

2026-09-08，我用脚本替换 `silent-risks.md` 的锚点 `--- / ## 怎么用这份清单`，
替换文本的结尾**没把锚点带回来**，于是那个小节标题连同分隔线被删掉。
正文的项目符号照常留着，渲染出来看不出异样，**全库测试全绿**。
是我后来核方法论编号时撞见的，不是任何守卫抓到的。
⚠️ **20260910 补上了那道守卫**：`TestMethodologyNumbersAreContiguous` 钉住方法论编号不重、不缺、从 1 起（红搭档 273 重号 / 274 缺号）。⚠️ 它防的不是「文档变难看」—— 编号是**引用地址**（代码注释里到处是「方法论第 28 条」），重号之后两条内容抢同一个地址，而**跳过去的人会读到其中一条、觉得讲得通，然后停下**。

评审随后独立扫了全部文档的**历史标题并集减去现有标题**，得到 13 个「出现过、现在没有」
的候选，逐条对下来**全部是改名、零丢失**（多数是证据等级变了导致标题跟着变，
例如「（**待实测**）」→「（**实测（快期模拟·取值）**）」）。我复核了这 13 条，结论一致。

⚠️ 但那个扫描**不该做成测试**：今天它 13 个候选全是误报，
做成硬门禁第一周就会被关掉——与 `restatement-count.sh` 注释里写的是同一种死法。

**改用小节数：改名不动这个数，删除会动。** 计的是 `^## ` 与 `^### ` 两级，不去重。

| 文档 | 小节数 |
|---|---|
| `README.md` | 6 |
| `docs/design.md` | 28 |
| `docs/cn-futures-rules.md` | 43 |
| `docs/fidelity.md` | 21 |
| `docs/probes.md` | 127 |
| `docs/roadmap.md` | 32 |
| `docs/ctp-oracle.md` | 12 |
| `docs/silent-risks.md` | 14 |
| `docs/state.md` | 21 |

⚠️ **增删小节时要同步改这里**，这是刻意的摩擦：一次有意的增删是一行改动，
一次意外的删除则会红。守卫是 `TestDocSectionCountsMatch`。

## 通路

| 键 | 值 | 出处 |
|---|---|---|
| `kq_login` | 已实测 2026-09-07 | [probes.md](./probes.md) §6.1 |
| `simnow_login` | 已实测 2026-09-07 | probes.md §6.3 |
| `dce_daily` | **未打通**（412） | probes.md §2 |
| `kq_vs_ctp_diff` | **已测量，差为 0；样本仍弱** | 2026-09-08 有持仓、有浮亏 −10 时 `balance == ctp_balance`、`available == ctp_available`，差 `0.000000`。⚠️ 但样本里**没有期权、没有昨仓、没有冻结**，这三样正是两个口径可能分岔的地方 |

## `doc_debt`：文档里写着、代码里还没有的标识符

⚠️ **规划可以先于代码，但那笔债必须是机械可见的**，而不是靠谁在消息里说一句
「这是我欠的」。当前 4 项，每项写明预定版本（`Version` 已随 `refdata` 落地，2026-09-07 移除）：

| 标识符 | 是什么 | 预定版本 |
|---|---|---|
| `Bar` | `Advance` 的入参 | v0.2.0 |
| `Advance` | 盘中推进 | v0.2.0 |
| `NotModeledUntil` | 对拍「不建模」声明的到期版本 | v0.2.0 |
| `Restore` | 状态恢复 | v0.9.0 |

⚠️ **第一次量出来的数是 77，那个数是错的。** 判据把 CTP 字段名、DIFF 字段名、
第三方 API（`ReqQryInstrument` / `TqKq` / `TrimSpace`）都算成了「本库该实现的」，
也不认识 `const` 与结构体字段。**差的 72 项全是判据的错，不是债。**

## `reject_priority_measured`：实测判出先后的那几对

⚠️ 八项校验两两共 **28** 对。下表是**判出来的**，其余的顺序在生产代码里
（`order.Check` 的取值顺序）**是猜的** —— 猜的部分与量出来的部分在代码上
长得一模一样，所以这张表要单独立着。

判据一律是「两组 `last_msg`（或 notify 码）**逐字不等**」，不认关键字。
原始夹具见 [probes.md](./probes.md) §14、§15。

| # | 同时违反的两项 | 柜台报的是 |
|---|---|---|
| 1 | 最小变动价位 × 涨跌停 | **最小变动价位**（⚠️ 这一对翻过一次案，见 `kq_facts` 44/45）|
| 2 | 最小变动价位 × 可平量 | 最小变动价位 |
| 3 | 涨跌停 × 可平量 | 涨跌停 |
| 4 | 合约可交易 × 最小变动价位 | 合约可交易（notify `code=311`，不是 `412`）|

⚠️ **测不了的几对也要说清是「测不了」而不是「没测」**：手数上下限只能与
可平量一起触发（超上限的开仓单会先被探针安全阀拦下，而调松安全阀去测一条规则
是拿真账户换一条观测）；可用资金不足要把账户打到接近爆仓；限仓要交易所额度。
见 [cn-futures-rules.md](./cn-futures-rules.md) §9。

## `simnow_pending`：等 SimNow 裁决的事项

⚠️ **这一节的存在，是因为它此前不存在。** 我在给评审的回复里写过「已进 SimNow
复核清单」——而那份清单当时**根本没有**，只有散在五个地方的行文提及。
一个承诺写在散文里，没有任何东西会在正确的时刻把它顶出来。

| # | 等什么 | 谁在等它 | 落在哪个版本 |
|---|---|---|---|
| 1 | 裸 `CLOSE` 在 `UseHistory` 交易所上是否真等于平昨 | `order` 的平仓校验；在此之前**报错**而非按平昨处理 | v0.4.0 |
| 2 | `CloseProfitByDate` 与 `CloseProfitByTrade` 两套口径 | `pnl` 包。天勤只给一个 `close_profit`，分不开 | v0.2.0 |
| 3 | 完整的 6 个手续费率 + 4 个保证金率 | `fee` / `margin`。天勤只给「每手」单一数值，**丢掉平今费率这个维度** | v0.1.0 |
| 4 | 快期模拟与真实 CTP 柜台的口径差 | 全库的措辞。⚠️ 空仓时两口径必然相等，**必须建仓后测** | v0.8.0 |
| 5 | CTP 的**数值**错误码 | `ctperr` 的取值。⚠️ 原写「快期模拟给的是中文文案」——**那句话不准**：20260909 发现快期的 `notify` 通道带数值码（311/401/412，见 kq_facts 47）。结论没变（那是**天勤自己的**码，不是 CTP 的 `ErrorID`），但理由要说准：不是「没有码」，是「码不是那一套」。⚠️ 一个理由说错了的待办，会让下一个人去找一个本来就不存在的东西 | v0.1.0 |
| 6 | **单向大边的合并范围**（品种 or 合约）（2026-09-08 新增） | `margin` 的 `SideScope`。⚠️ 快期模拟**根本没实现大边**，此问题在那个口子上测不出来，不是「还没测」而是「测不了」 | v0.1.0 |
| 7 | 保证金 / 手续费的**基准价**（2026-09-08 新增） | 全库。⚠️ 快期用昨结算价，**真实 CTP 的按额手续费用成交价**；这是已知偏离，必须由第二个口子定 | v0.8.0 |
| 9 | ~~真实柜台的 `CThostFtdcBrokerTradingParamsField`~~ ✅ **已取到（20260909 13:3x）** | 自写 syscall 绑定查到（`goctp` 不导出这个查询）。**`MarginPriceType="4"` 开仓价、`Algorithm="2"` 只计浮动亏损、`AvailIncludeCloseProfit="0"` 包含平仓盈利**，币种 CNY。见 probes.md §6.5。⚠️ 原写的**三条界限**里，第一条**已经解掉**：~~它是**声明**不是行为（账户空仓，没验过它真按这个算）~~ —— **20260910 夜盘用 `ctp-hold` 验了行为，与声明一致**：持仓期间今结算价动过而占用保证金一次没动，三个成交价各自反解出整整 16.0000%（probes.md §6.8）。⚠️ 这是 ctp-oracle.md §3 那条组织原则（CTP 同时给声明与行为，故能验「声明是否等于行为」）**第一次真的兑现**。剩下两条界限仍在：它是 **broker 9999 一家**的配置（字段名就叫 BrokerTradingParams，按柜台配是定义本身）；因此**不推翻** kq_facts 1。⚠️ 它当场推翻的是**本库候选集的形状**：`margin.PriceBasis` 四个候选里有两个不在 CTP 的取值空间内，而 SimNow 实配的 `OpenPrice` **本库表达不了** —— 候选集是「想出来的」不是「照柜台枚举列的」。⚠️ 顺带给了 `simnow_pending#4` 第一份硬数据：`Algorithm` 快期 `"1"` / SimNow `"2"`，**口径差是存在的，且就在最核心的参数上** | v0.2.0 |
| 10 | **部分平仓时 `OpenCost` 按什么冲减**（2026-09-09 新增） | `position` / `pnl`。⚠️ 实测快期**按持仓均价**冲减：今1/昨3 @3159.75 平昨 1 手之后仍 @3159.75、`open_cost = 3159.75×3×10`；而本库保留**逐笔明细**，消耗的是具体那一笔，均价因此变成 3162.6667。⚠️ 这不是算错，是 design.md 决策 10 与柜台实现的分岔 —— **均价是有损压缩**，柜台丢掉信息之后它的 `float_profit` 在部分平仓后不再是真正的逐笔对冲口径。本库不改，等真实 CTP 裁决 `OpenCost` 的冲减口径 | v0.8.0 |
| 11 | **报单校验的拒绝优先级**，以及**非整数倍价格该不该被拒**（2026-09-09 新增） | `order` 的 `Check` 取值顺序与 `checkPriceTick`。⚠️ 前者快期与文档表一致（kq_facts 44），但只判出了少数几对（见本页 `reject_priority_measured`）；后者快期**收下**零头 ≥ 半个 tick 的价格（kq_facts 45），而本库按交易所口径**一律拒绝** —— 这是一处已知偏离，等真实 CTP 定 | v0.4.0 |
| 8 | 昨仓相关的一整批（2026-09-08 新增） | §13 的 #1/#2/#4/#7/#8/#10。⚠️ 它们不是「难测」，是**必须隔一次结算**——过夜种子已建 | v0.2.0 |

⚠️ 第 5 条是 `rules_measured = 0` 的原始原因：实验 6 只收敛了快期模拟那一半。

⚠️ 第 6 条的形状与其余各条**不同**，值得单记：其余是「还没测」，它是「**这个口子测不了**」。
两者在待办清单上长得一样，处理方式却相反——前者排期，后者要换工具。
把「测不了」记成「还没测」，会让人一次次回到同一个口子上重跑同一条实验。

## 已知且已裁决的历史遗留

| 项 | 状态 | 裁决 |
|---|---|---|
| TqSdk 的 `client_secret` 出现在 `d76f4b1`、`3872d9d` 两个已推送 commit 里（仓库 public） | **不重写历史**（2026-09-07，使用者裁决） | 见下 |

理由，逐条可查：

1. **那个值本来就是公开的**——它在 `shinnytech/tqsdk-python` 的 `tqsdk/auth.py`
   里，本库正是从那里读到的。它是**该 SDK 分发给所有使用者的客户端身份**，
   不是任何人的凭据，单凭它拿不到任何账户。
2. **重写的代价是实的**：`dev` 与 `main` 全部 SHA 作废，评审会话正在追踪的引用
   全部失效。
3. ⚠️ **重写的收益是虚的**：兄弟仓 `futures-tickflow-go` 做过重写并**当场复验**——
   旧 SHA 经 GitHub API **仍返回 200**（GitHub 对弃置对象保留一段时间）。
   所以那是「大幅降低」不是「彻底消除」。
4. **向前不再分发已经做到**：当前代码从 `.env` 读，缺失时报错，
   `git grep` 与 `git log -S` 均已复核。

⚠️ **措辞按第 3 条来。** 本文档不写「已彻底移除」——那会是又一次
「声称做过某事」而没有验证路径，正是 [silent-risks.md](./silent-risks.md)
方法论第 5 条要防的东西。

## 派生禁语

各状态键翻转后，下列字符串不得再作为**现时叙述**出现（只允许出现在
`~~删除线~~`、`> ✅` 横幅、或 `<!-- 历史留档 -->` 块内）：

| 键 | 禁语 |
|---|---|
| `kq_login` `simnow_login` | 尚未实际登录 / 待注册 / 柜台账号还没有 / 需要账号 / 待用户提供 / 外部依赖：已解除 |
| `rules_pending` | 六条 / 6 条待实测 / 七条 / 7 条待实测 |
| `position_fields` | 持仓 28 字段 |
| `packages_done` | 文档阶段，尚无实现 / 尚无实现 / 本文现在写的是**将要成立的条件** / 核算逻辑尚未落地 / 只有包声明与文档守卫 |

⚠️ **扫描范围：`README.md`、`docs/*.md`，以及 `doc.go`。**
最后一个是 2026-09-09 补的：同一条低报的**第三处**藏在包注释里，
躲过了这张表整整两天 —— 而**包注释是这个库最公开的一句话**，
`go doc` 与 pkg.go.dev 显示的就是它，比 README 还先被看到。
⚠️ 只加 `doc.go`，不加全部 `.go`：那会把「禁语」变成「禁词」，
而代码注释里讨论这些字符串是正当的。

### ⚠️ 自指豁免必须在写脚本**之前**定死

拿上面这张表在全库真跑一遍，命中 **6 处**，全部合法：

| 位置 | 为什么合法 |
|---|---|
| `probes.md` 两处 | 历史留档，记录「当时缺什么」 |
| `roadmap.md:143` | **列举禁语的那一句** |
| `roadmap.md:180` | 分析「哪条机制抓得住哪一类」的表 |
| `roadmap.md:354` | 变更记录引用历史原文 |
| 删除线内一处 | 已显式作废 |

⚠️ **一个永远红的检查会被忽略，被忽略的检查等于没有检查。**
它和「有人去刷 `restatement-count.sh` 那个数」是同一种死法——
只是这次死于**噪声**，那次死于**粉饰**。

所以豁免规则现在就定死，**不等它红了再补 `# nolint`**——补出来的豁免会越加越宽：

> **禁语只在「正文叙述行」上生效。** 一行是正文叙述行，当且仅当它
> ① 不在代码块（``` 围栏）内；
> ② 不在历史留档块内。⚠️ **标记的判定是「整行就是它」，不是「行内含有它」**
>   ——本文件在定义标记时必然要提到它们，按「含有」判定会把定义行读成未闭合的
>   块，于是本文件结束时深度不为零。这与「字典必然包含它定义的每一个词」是
>   同一个形状，只是从禁语层跑到了标记层，而它暴露的是**「怎么算一个标记」
>   当时根本没写**——两个实现可以在这里分道扬镳，且两条都读得通；
> ③ 不含删除线 `~~`；
> ④ **位于本文件（`state.md`）内，或含指向 `state.md` 的链接**。

第 ④ 条的后半句是刻意设计的：讲这套机制的段落**本来就该引用唯一来源**。

⚠️ **前半句是补上的，而它暴露了一个形状**：唯一来源建起来之后，
它成了全库**唯一必然违反禁令的文件**——字典必然包含它定义的每一个词。
而规则是在它存在之前写的，所以漏掉了字典自己。

⚠️ 更糟的是这条排除起初**只存在于实现里**：我第一版检查器写了
`if p.endswith('state.md'): continue`，规则里一个字没有。
于是我跑出 0 处、评审跑出 4 处，**分歧的根源不是实现有 bug，是规范缺一条**。
于是豁免不是特批，是**挣来的**——你要谈禁语，就得同时把读者指回 `state.md`。
⚠️ 历史留档的标记也据此从散文（「以下保留原文」）改成了机器可读的 HTML 注释；
散文标记脚本读不懂，而读不懂的结果是**误报**，误报的结果是被忽略。

### 落成脚本时的「守卫的守卫」

标记本身会写错。2026-09-07 `probes.md` 就出过 `start=3 / end=2`——
把标记「提到标题之前」时加了新的、没删旧的。

⚠️ **这个毛病的失败方向朝着「什么也不报」**：深度计数式实现下，
`probes.md` 结束时 depth=1，若不逐文件重置，按字母序排在它后面的
`roadmap.md` / `silent-risks.md` / `state.md` **会被整体豁免掉**。
0 处不是因为干净，是因为检查在半路闭了嘴。

**一个豁免机制出 bug，比检查本身出 bug 更糟：它让检查更安静，不是更吵。**

所以脚本必须自己断言：

1. 每文件 `start == end`，**不配平直接失败并指出文件名**
2. 不允许嵌套（深度只能是 0 或 1）
3. EOF 时深度归零
4. **逐文件重置块状态**，绝不跨文件带

⚠️ **禁语只防复发，不防首发。** 它是一份对自然语言的黑名单，而黑名单漏一条即静默失效。
禁语从**状态翻转那次提交里被删掉的字符串**中生成——列表由真实编辑长出来，
不由想象力长出来。**首次写出一句新的过期表述，仍然只能靠人眼。**
