# 版本排期

范围与决策依据见 [design.md](./design.md)，规则依据见 [cn-futures-rules.md](./cn-futures-rules.md)，
可行性依据见 [probes.md](./probes.md)。

## 分支与发布约定

| 分支 | 角色 |
|---|---|
| `main` | 只接受来自 `dev` 的合并，每次合并打 `vX.Y.Z` tag，保持永远可 `go get` |
| `dev` | 默认开发分支，日常提交落这里 |
| `feat/*` `fix/*` | 仅当改动会持续多天且打断 dev 时才开，完事 squash 回 dev |

Go 库特有约束：

- **tag 必须打在 `main` 上**，严格 semver（`v0.1.0`），`go get` 只认这个
- **v1.0.0 之前 API 可自由变更**（v0 不受兼容性承诺约束）——要充分利用这个窗口把 API 打磨定型
- 一旦发布 `v2`，module path 必须加 `/v2` 后缀。**因此 v1.0.0 是一道必须谨慎跨过的门**

⚠️ 本机 `proxy.golang.org` 不可达（[probes.md](./probes.md) §1），
开发与 CI 必须 `go env -w GOPROXY=https://goproxy.cn,direct`。

---

## 排期原则：证据先于代码

参照项目的经验里，代价最高的缺陷都不是写错公式，而是**把一个猜测当成事实写进了代码**，
然后所有测试都在验证那个猜测。所以本项目的排期有一条硬规则：

> **每个版本先跑判别实验，再写实现。**
> 实验的原始输出存进 `testdata/`，结论写回 cn-futures-rules.md 并把证据等级
> 从「待实测」改成「实测」。**没有实测支撑的规则，代码里要有显式标注和守卫测试。**

cn-futures-rules.md §13 列出的**全部待实测项**（当前条数见 [state.md](./state.md)）都落在 v0.1.0。
在它们收敛之前，v0.2.0 的核算公式无从确定。

---

## 版本计划

工作量为相对权重（1 = 最小单元），不是日历天。

| 版本 | 内容 | 验收 | 权重 |
|---|---|---|---|
| `v0.1.0` | **规则数据层 + 判别实验**：`types` / `ctperr` / `refdata`（合约规格、保证金率、手续费率、交易日历与时段、Provider 抽象、快照持久化、内置快照）、`refdata/live`、`cmd/refdata-sync`、**`cmd/oracle`（嵌套模块，`probe` 子命令跑判别实验）** | [state.md](./state.md) 里 `rules_pending` 的**全部**待实测项收敛为「实测」（带双坐标），原始输出入库 | 5 |
| `v0.2.0` | **单合约投机核算 + 逐日盯市结算**：`fee` / `margin` / `pnl` 三个纯函数包、`position`（含逐笔明细**与今昨仓划分**）、`account`、**完整**的日终结算链路（含第 7 步今仓转昨仓；⚠️ 实现在 `position.Settle`，`settle` 包已并入，见下方变更记录）、`view`、对拍的**到期机制** | 与快期模拟逐字段对拍，**三态验收**：**账户与持仓的全部字段**（数量以 [state.md](./state.md) 为准，⚠️ 不在此处抄数）落进「一致且被触发」或「已声明不建模且带到期版本」；**未触发**的列白名单并说明何时能触发 | 7 |
| `v0.3.0` | **平今平昨的报单语义**：~~`PositionDateType` 两条链路~~ ✅ **已落地（20260909）**：`position.New` 带上它、`Settle` 按它分岔（UseHistory 滚今昨、NoUseHistory 只推进基线），零值结算报错；⚠️ 清单上剩的三项，20260909 复核后**一项已做、两项被这个口子堵死**：**显式平今/平昨声明已落地**（`position.Close` 带 offset 且今昨分别校验，`order.CheckClosable` 同）；**平今费率**在这个口子上与开仓同值、分不开（kq_facts 18/34，`fee` 的档位代码在、只缺能区分的语料）；**消耗顺序**结构性测不出（kq_facts 40）。⚠️ 「做不了」与「没做」在清单上长得一样而处理方式相反 —— 前者要换口子，后者才排期| 隔夜持仓的完整对拍：开仓 → 结算 → 次日平今平昨，费用、**平仓盈亏**与持仓逐字段一致 | 4 |
| `v0.4.0` | **报单与撮合**：~~`order` 的八项校验与冻结~~ ✅ **已落地（20260909）**；⚠️ `match` 的限价/市价、成交角色判定 —— **20260909 使用者已裁决**：不做盘口，默认 **100% 全量成交**。⚠️ 偏离有两个维度且**方向相反**（价格保守 / 成交与否乐观），且**无法被本库的对拍验证** —— 见下方变更记录与 silent-risks 第 9 条。涨跌停与最小变动价位已在 `order` 的八项校验里，那两项**能**验证；⚠️ **`UseHistory` 合约上收到裸 `CLOSE` 必须报错**（依据 cn-futures-rules.md §4，且 [state.md](./state.md) 的 `simnow_pending#1` 未裁决前不得改成按平昨处理） | 冻结额与 `frozen_margin`/`frozen_commission` 一致；被拒报单的错误码一致；裸 `CLOSE` 报错有守卫测试 | 4 |
| `v0.5.0` | **多合约多品种 + 单向大边**：跨合约跨品种的保证金合并、`MaxMarginSideAlgorithm`、多空分率 | ⚠️ **必须用同品种两个不同月份**建多空验证大边合并范围，单合约测不出来 | 3 |
| `v0.6.0` | **风控与结算单**（`risk` 包在此版本落地）：风险度、追保判据、可选的约定强平策略（默认关闭）、结算单生成 | **结算单逐项对拍**——它是逐日盯市的最终陈述 | 3 |
| `v0.7.0` | **交割月与到期**：交割月保证金递增、自然人持仓限制告警、合约到期处理 | 跨越交割月前一个月的完整回测，保证金阶梯逐档一致 | 2 |
| `v0.8.0` | **对拍加固与差异清零**：双口子全字段对拍、修复暴露的全部差异、性能基准、并发形态验证 | 有差异 0、未分类 0、**未触发 0**、**未摘的到期声明 0**；天勤与 CTP 的口径差被测量并记录 | 5 |
| `v0.9.0` | **API 打磨**：命名与签名定型、向后兼容审查、包文档、示例、导出面金文件（golden file）守卫 | 导出方法、导出字段、**导出枚举取值**三重守卫 | 2 |
| `v1.0.0` | **冻结**：API 冻结，fidelity.md 与 silent-risks.md 定稿 | 全部测试绿 + 全部对拍夹具入库 | 1 |

---

## 对拍的「不建模」必须带到期版本

验收规则是「每个字段要么值对得上，要么写明为什么不建模」。这条规则有个**可以被合法
绕过**的出口，v0.2.0 恰好会撞上：

账户的 `frozen_margin` / `frozen_commission` / `frozen_premium`、持仓的
`volume_*_frozen_*` ~~与 `order_volume_*`~~ —— 要到 v0.4.0 有了

⚠️ **`order_volume_*` 那四个划掉了（2026-09-09 核实）**：427 个持仓截面里
**一次都没出现过** —— 柜台根本不发它们。它们在 `positionKeep` 白名单里，
是照 DIFF 文档列的，而文档列了不等于口子会发。
⚠️ 把它们算进「要实现的字段」会让人去做四个**无法被验证**的实现：
没有观测就没有对拍，写成什么样都不会红。

⚠️ 同一形状的还有委托记录里的 `frozen_margin`（三份样本全缺席）。
两处都留在白名单里是对的 —— 哪天出现不该被判成漂移 —— 但**不该被当成欠债**。

剩下的那些要到 v0.4.0 有了
`order` 与冻结才可能实现。于是 v0.2.0 想让对拍全绿，**唯一的路径就是把它们声明成
「不建模」**。而它们恰好是 [silent-risks.md](./silent-risks.md) 第一节第 7 条点名的那批：
报单冻结不建模 → 回测比真实账户「有钱」→ 开得出实际开不出的仓。

⚠️ ~~**声明一旦写下去，没有任何机制逼它在 v0.4.0 被摘掉。**~~
**✅ 已补上（自然日 2026-09-08）：** `Version` 常量 + `TestNotModeledDeclarationsHaveNotExpired`。
它扫全仓的 `view.Skip("vX.Y.Z", …)` 与 `NotModeledUntil: "vX.Y.Z"`，
只要某处的到期版本**已经不晚于** `Version` 就红。把 `Version` 往前推一个数，
会当场逼出全部到期的欠账 —— 那正是发版时该发生的事。

⚠️ 三处细节，都是破坏验证逼出来的：

- **解析不了的到期版本 = 永远不会到期的豁免**，所以解析失败**判红**而不是跳过。
- **「等于当前版本」那一档在真实声明上分不开**（没有一处恰好等于 `v0.1.0`），
  所以单独写了一条只考验相等的单测 —— 而它正是最要紧的那一档：
  v0.4.0 发版时，「不建模到 v0.4.0」的那些必须当场到期。
- **扫描有两条独立的臂**（调用式与键值式），合计下界会掩盖单臂失效：
  把调用式那条打断，声明数从 10 掉到 8 仍然过界。改成**分臂各设下界**。

原文保留： 一个「暂缓」会安静地
变成「永久」，而且是在测试全绿的状态下变的。

因此 `policy.go` 的每条「不建模」**必须带一个到期版本**：

```go
{Field: "frozen_margin", Reason: "报单冻结未实现", NotModeledUntil: "v0.4.0"}
{Field: "premium",       Reason: "期权权利金，v1.0 不覆盖", NotModeledUntil: "never"}
```

对拍工具跑在该版本**之后**仍见到未摘的条目，**判失败**。
把「暂缓」和「永久不建模」在制度上分开，成本是几行代码。

⚠️ **硬约束：第一条「不建模」声明与它的到期校验必须落在同一个提交里。**
不允许先有声明、后补校验——否则这条「防止暂缓变永久」的机制自己就先处在暂缓状态，
而那正是它要防的东西。

---

## 对拍的第二个洞：零值假通过

「不建模」的出口堵上了，还剩一个：**字段在两边都是零、且本次样本从未触发它，
对拍照样判「一致」。**

本仓库第一次连上快期模拟就撞见了它：账户空仓，`balance` 与 `ctp_balance`
都是 `1000000.0`，两个口径「完全一致」——而空仓时**任何**两种权益口径都相等，
这什么都没证明。

一般化之后它吃掉一大片：`close_profit`、`commission`、`volume_long_his` 这些
只在特定动作后才非零的字段，只要对拍样本没真的开仓、结算、平仓走一遍，
就会**以零值静默通过**。

它与「阴性结果不能当阳性用」是同一件事，也与「对拍测试零次迭代也会绿」是同一条原理
——只是层级从「测试跑了几次」抬到「**字段被触发过几次**」。

因此验收从两态改**三态**：

| 档 | 含义 | 算不算通过 |
|---|---|---|
| 一致 | 值对得上，**且本次样本内被触发过** | ✅ |
| 不建模 | 已声明且带到期版本 | ✅ |
| **未触发** | 值对得上，但从未被触发 | ⚠️ **不算**，单独计数 |

触发判据从简：该字段在本次样本内出现过非零值，或出现过变化。
v0.2.0 允许若干未触发（白名单 + 说明何时能触发），**v0.8.0 必须为 0**。

### 字段集漂移也要自己报出来

23 个账户字段是**一次**快照数出来的，字段集未必稳定。别靠定期人工再数一遍，
**把字段集本身变成对拍的一部分**：每次运行断言「收到的字段集 ⊆ 已知字段集」，
多出来的字段判**未分类**（即失败），而不是忽略。

这是参照项目那条经验的自动化版本——它那次是靠人补全字段对拍，才一次暴露出
35 处差异与 4 个从未建模的字段。让它变成常态，就不必再指望有人想起来去数。

---

## 两条 v0.1.0 要一并落地的机制

它们与前面两条（到期版本、三态验收）是**同一个形状**：一个事实有唯一的家，
而复述它的地方与那个家之间没有链接。前两条都靠「加一个机械断言」解决，这两条同理。

### 1. 单一状态源 + 禁语检查（治「改状态改不干净」）

项目状态只在 `docs/state.md` 一张表里断言：

```
kq_login        已实测 2026-09-07
simnow_login    已实测 2026-09-07
rules_measured  0/6
dce_daily       未打通
```

每个状态键**派生**一组禁语——该状态翻转后就不得再作为现时叙述出现的字符串
（`kq_login` 的禁语：「尚未实际登录」「待注册」「柜台账号还没有」…，完整表在 [state.md](./state.md)）。
主模块里一个只用 stdlib 的 `doc_test.go` 扫全部 `.md`：禁语只允许出现在
显式标记的历史留档里（`~~` 删除线、`> ✅` 横幅之内、`<!-- 历史留档 -->` 块内），
出现在别处就红。

守卫的守卫：断言状态表非空，且**每个「已实测」的键至少有一条禁语**。

⚠️ **但必须承认它的边界，否则它会变成一种虚假的安心。**
禁语表是**对自然语言的黑名单**——而黑名单漏一条就静默失效，这正是下面第 2 条
要拒绝黑名单的理由。中文里「尚未登录」有无穷多种说法，一条**新造的**过期表述
照样能穿过去。

所以它防的是**复发**，不是**首发**：

> 禁语从**状态翻转那次提交里被删掉的字符串**中生成，而不是靠人凭空想。
> 「这次改掉的话，以后不许再出现」——列表由真实编辑长出来，不由想象力长出来。

首次写出一句新的过期表述，仍然只能靠评审的人眼抓。**这一点要写在检查的注释里**，
免得后人以为绿了就等于没有过期陈述。

#### 另一半：减少复述面（这一半治首发）

上面四处过期陈述有个共同点：**它们全是复述。** 事实只有一个家
（`probes.md` §6/§7），另外几份文档各自用散文把它再说一遍。
禁语治的是复述**变质**，没治**复述本身**。

所以第二条机制是：**其他文档不断言状态，只指向那个家。链接不会过期，
因为它不含事实。**

⚠️ **它是白名单形状的，而且绕开了「白名单建不在措辞上」这条反驳** ——
措辞无穷，但**位置有限可枚举**：状态断言只准住在 `state.md` 与 `probes.md §7`。

两条机制**失败在不同的用例上**，这才是并用的理由：

| 过期陈述 | 禁语 | 减少复述面 |
|---|---|---|
| `probes.md` §5 那句、`cn-futures-rules.md` 两处（同批字符串复述） | ✅ 抓得到 | ✅ |
| `design.md` §7 整节「待用户提供」（不含任何现成禁语，禁语表见 [state.md](./state.md)） | ❌ 抓不到 | ✅ **那一节存在的唯一理由就是复述阻塞状态；若它当初是一条链接，就没有东西会过期** |
| 新造的措辞、新写的一节 | ❌ | ⚠️ 只在「新写的是纯状态节」时抓得到 |

**并用仍不是 100%**，但两者基于不同原理、不会一起失效——与脱敏的
「白名单 + 独立复查」是同一个理由。

#### 分工由「复述有没有就地含义」决定

完全消灭复述会付出可读性代价：`refdata` 来源表里的
「✅ 已登录，见 [probes.md](./probes.md) §6.3」若砍成光秃秃一条
「见 §6.3」，读者要跳一次才知道能不能用。所以分工是：

- **纯状态复述**（一节/一行的唯一职责就是报状态，如 `design.md` 旧 §7）
  → 删掉，换成链接。归**位置白名单**管
- **嵌在论证里的状态词**（如来源表里的 ✅ 与结论并排）
  → 保留，但它从此落在**禁语的爆炸半径**内

**当前散落面已量出来：`probes.md` 之外还有 23 处状态断言。**
v0.1.0 的任务不是清零，是把纯状态复述那部分清掉，并让剩下的全部落进禁语覆盖。
清零本身要有个数字可查，否则「减少复述面」会变成一句无法证伪的口号。

### 2. 脱敏用白名单，不用黑名单（治「夹具泄漏」）

判别实验的原始 JSON 要入 `testdata/probes/`，而 `snapshot.json` 里有账户 UUID。

| 写法 | 漏一个字段会怎样 |
|---|---|
| 黑名单（擦掉已知敏感字段） | 泄漏，**静默** |
| **白名单**（只留已知安全字段） | 该字段从夹具消失 → 对拍报「缺字段」→ **响** |

同一个疏忽，一个静默一个响。这与「字段集断言 ⊆ 已知集」是同一条原理的另一半：
那条管**多出来**的，白名单管**该去掉**的。

三条配套：

1. **脱敏在落盘之前做。** 先落原始盘再擦，原始文件已经上过磁盘、可能已进过
   git index —— 一次 `git add -A` 就够了
2. **脱敏后断言字段集恒等**：`夹具字段集 == 已知字段集 − 白名单外集合`，任何漂移直接红
3. **再加一条独立机制的复查**：断言夹具里不含 UUID 形状的串、不含 `.env` 里那几个值。
   它与白名单是**两套不同原理**，因此不会一起失效 —— 一个 bug 同时骗过两种不同
   机制要难得多

---

## ⚠️ 今昨仓前移带出的一条约束

今昨仓的划分与滚动前移到 v0.2.0 之后，有一件事**仍然**要等 v0.3.0：
**显式的平今 / 平昨声明**。于是在 `UseHistory` 合约上，**v0.2.0 平不掉昨仓**。

后果必须写明，否则正好掉进上面那个零值假通过的洞：

- v0.2.0 **能验**：昨仓基线出现在**持仓盈亏**里（结算后昨仓以昨结算价计）
- v0.2.0 **验不了**：**平仓盈亏**的平昨基线 —— 因为平不掉昨仓
- 因此 `close_profit` 在 v0.2.0 **只能靠平今触发**，平昨那条分支要到 v0.3.0 才有样本

这一条进 v0.2.0 的未触发白名单，到期版本 `v0.3.0`。

---

## v0.1.0「不下单的那一半」的四条约束

⚠️ **它不叫「只读的那一半」。** `goctp` 的登录链路里，`onLogin` 收到成功应答后
**立刻调 `ReqSettlementInfoConfirm`** —— 确认结算单是**写操作**，不是查询，
而且它在 2026-09-07 的 SimNow 探针里**已经发生过了**。

这不构成问题（模拟盘上确认结算单是常规动作，不确认根本不能交易，离下单很远），
**但标签必须准**：一个高估自身安全性的标签，正是本项目从头到尾在防的东西。
若日后被问「等点头的这段时间碰了账户什么」，两个名字给出的答案不一样。

### 1. `ctperr` 的取值一个都不许先填

roadmap 自己写着实验 6 的产出就是「`ctperr` 的取值」。先写等于把一批猜的错误码
钉进 v0.1.0 的交付物，而 v0.1.0 的验收恰恰是「全部待实测项收敛为实测」——
与「**每个版本先跑判别实验，再写实现**」正面冲突。

包骨架、文件、类型定义可以先有，**取值一个都别填**。
⚠️ 2026-09-07 已拿到**快期模拟**的拒因文案，但那不是 CTP 的数值错误码，
不能用来填 `ctperr`。

### 2. `refdata` 的品种索引：建它是因为它便宜，不是因为实验 3 有了结论

若大边按**品种**合并，`refdata` 必须能 contract → product 并枚举同品种全部合约；
若按合约，不需要。这个索引很便宜，且解析 CZCE 的三位月份本来就要它，所以**直接建**。

⚠️ **但代码注释与文档里必须写明这条理由。** 否则实验 3 真跑出「按品种」时，
这个索引会被读成「当初就验过了」——一个未经验证的假设就这样变成既成事实。

### 3. 这半边的产出同样要脱敏，从第一次写盘起

别以为不碰账户截面就没有标识信息。实测 CTP 结构体：

| 结构体 | 标识字段 |
|---|---|
| `CThostFtdcInstrumentField`（合约表） | **无**，干净 |
| `CThostFtdcInstrumentMarginRateField` | `BrokerID` `InvestorID` `InvestUnitID` |
| `CThostFtdcInstrumentCommissionRateField` | `BrokerID` `InvestorID` `InvestUnitID` |

⚠️ **费率是按投资者查的，应答里自然带着你是谁。** 白名单脱敏在这半边就要生效。

### 4. 拉到规则数据 ≠ 规则被实测

见 [cn-futures-rules.md](./cn-futures-rules.md) 表头的**双坐标证据等级**。
拉规则数据能让一大批字段升到「实测（取值）」，**而实验 3/4/5 一条都不会因此收敛**。

⚠️ **计数也是状态。** 验收行不许抄「N 条」「N 字段」，一律写「全部」并链接
[state.md](./state.md)——2026-09-07 已经因此栽过两次，两次都躲过了禁语检查，
因为**状态词至少是词，人眼会停；计数是数字，人眼扫过去根本不会停**。

---

## v0.1.0 的判别实验清单

这是当前唯一在跑的版本，它的核心交付物不是代码，是**证据**。
实验全部来自 cn-futures-rules.md §13（条数见 [state.md](./state.md)），每条都已写好判别方法：

| # | 问题 | 判别方法 | 阻塞什么 |
|---|---|---|---|
| 1 | 盘中保证金用哪个价 | 持仓不动，等行情走 1%，看保证金是否变；再用「今仓开仓价 ≠ 昨结算价」的一笔分开候选 1 与 2 | `margin` 包的全部公式、可用资金曲线、追保时点 |
| 2 | 盘中持仓盈亏用哪个价 | 持一手昨仓，看盈亏是否随行情变（候选 2 会恒为零） | `pnl` 包、风险度 |
| 3 | 单向大边按品种还是按合约 | **同品种两个不同月份**分别建多空，看合计是 `max(多,空)` 还是 `多+空` | `margin` 的合并范围，跨期套利可能差一倍 |
| 4 | `NoUseHistory` 合约的平仓消耗顺序 | 隔日建昨仓，次日开今仓，平掉**少于昨仓量**的手数（平满了三种顺序同值）。**同时看两个字段**：`Commission` 与 `CloseProfitByDate` —— 后者的差通常比手续费**大一个量级**，更容易把候选分开 | `fee` + **`pnl`** + **`account`**：消耗顺序换一个，平仓盈亏的基线（平昨用昨结算价 / 平今用开仓价）跟着换，当日盈亏与结存链条一并受影响 |
| 5 | 手续费的取整口径 | 构造理论值恰在半分附近的成交，看实际值 | `internal/decimalx`，长周期结存漂移 |
| 6 | 报单被拒的错误码 | 逐项触发八类校验失败 | `ctperr` 的取值 |

### 实验之间的时间依赖

⚠️ **昨仓造不出来，只能等一次结算。** 夜盘 21:00 之后属于下一个交易日，
但中间**没有结算**；真正把今仓变成昨仓的是日终结算。所以：

```
D 日   oracle probe -exp overnight-setup     建种子（此时全是今仓）
D 日日终  （交易所结算）                        今仓 → 昨仓，昨结算价 ← 今结算价
D+1 日 baseline-full / close-order / yd-vs-his
```

| 实验 | 何时能跑 | 今仓下能得到什么 |
|---|---|---|
| 6 报单拒因 | 任何时候，**不需要成交** | ✅ 已收敛（快期模拟侧） |
| 3 单向大边 | 当日，需两个同品种不同月份 | ✅ 完整 |
| 1/2 今仓版 | 当日 | ⚠️ **只能排除「连续重估」一个候选**，不得写成收敛 |
| 1b/2 完整版 | **D+1** | 反解出 position_profit 的基线；比较今昨每手保证金 |
| 4 平仓消耗顺序 | **D+1** | ⚠️ 本样本**分不开**「先平昨」与「先开先平」，需连续两日各建一次种子 |
| 7 yd vs his | **D+1** | ⚠️ 二者相等时**分不开**，需平掉一手昨仓再看 |
| 5 手续费取整 | 需 CTP | 天勤只给「每手手续费」，六个费率要 SimNow |

### ⚠️ 当前挂着的两件、各自等什么（2026-09-09 记；采样目的 07:xx 改写）
    ✅ 日盘那件**已完成**（20260909 09:02，见下方小节末尾）
      边界钉在 0.5，盘中盘后一致；守卫换成双向断言
    等今天日终结算（约 15:00–16:10）
      种子已在账上：SHFE.rb2701 多今1/多昨2、**空今1**
      ⚠️ 别在结算前把这些仓平掉：种子没了就要再等一天
#### ⚠️ 结算后那一次采样：目的**已改写**，因为原来的写法会让样本构造不够
原先记的目的是「取 `volume_short_frozen_his`，它是持仓侧冻结最后一个零观测字段」。
⚠️ **那个目的写小了。** 横读全语料之后（kq_facts 51）真实分量在这里：
    466 条持仓记录里，volume_short_his / volume_short_yd /
    pos_short_his / volume_short_frozen_his  **>0 的次数各为 0**
    —— 四个字段都在场，不是缺字段；**空头昨仓从来没有存在过**
    而空头**今**仓有 27 条
于是 kq_facts 28/33 那条「今昨拆分在结算时按 `PositionDateType` 写一侧」，
⚠️ **全部非零证据都在多头侧**，空头侧一次都没有贡献过非零观测。两个假说 ——
    A  规则是方向中性的，只是空头昨仓从未出现过（**本库建的是这个**）
    B  柜台只在多头侧写这个拆分
—— 在当前语料上**给出完全相同的预测**。
> **所以那不是补一个字段，是第一次让那一格具备判别力。**
⚠️ 目的因此登记为：**分开假说 A 与 B**，而不是「取一个没取过的字段」。
两者对样本的要求不同 ——
    按「取字段」构造   只要 volume_short_frozen_his 出现非零即可
    按「分开 A/B」构造 ⚠️ 必须**空仓活过结算**：今仓空头不过夜完全无效
                      （语料里已经有 27 条今仓空头，一条也没给出信息），
                      过夜之后还要在**有空头昨仓**的状态下再拍一次持仓截面
**目的写错了，样本就可能构造得不够。**
⚠️ 这一次不靠人记得去看：`TestShortHistoryCostHasNeverBeenObserved` 是条绊线，
它会在那一格第一次拿到值的时候**变红**，并在失败信息里说清该做什么。
红了要做的是判定 A/B、写进 `kq_facts`、然后**删掉绊线** —— 不是给它加例外。
#### ⚠️ 09:00 那次：原来写的目的是错的，按时段表逐笔核过

原文写着「系统性扫描全是收盘后跑的，**盘中只有一个点**与该模型相符」。
⚠️ **那句话不对。** 拿 `sessions-20260908.json` 逐笔判下来（守卫
`TestCounterHalfTickBoundaryHoldsInSession`），`SHFE.ag2702` 在它自己的夜盘时段内
（21:00–02:30，实测 02:09）有**两个不同零头**的点，而且分居半个 tick 两侧：

    零头 1/3  →  柜台报「下单价格不是价格单位的整倍数」
    零头 0.7  →  柜台报「价格超出涨停板」

差别是实质性的：一个点只能说「盘中没有反例」；
**两个跨过边界的点**说的是「盘中这条路径的走向与盘后一致」。
⚠️ 也就是说原来那个理由（「盘中盘后是不是同一条路径」）**基本已经答过了**。

⚠️ 但**边界的位置**仍然只有盘后样本：1/3 与 0.7 之间是空的。
落在 `(0.34, 0.69)` 里的任何一个边界都同样解释得了现有的盘中样本 ——
半个 tick 这个具体位置，盘中一个观测都没有。

> **于是目的改成：把零头扫到 0.5 附近，把那段空档填上。**

案例表已经补好（`exp_reject_tick_limit` 的 2/5、45/100、1/2、55/100、3/5）。
跑法：

    cd cmd/oracle
    go run . probe -env ../../.env \
      -specs ../../testdata/refdata/specs-20260908.json \
      -exp reject-tick-vs-limit -symbols SHFE.ag2702 \
      -dump ../../testdata/probes

⚠️ 合约选 `SHFE.ag2702` 不是随手挑的：现有那两个盘中点（1/3 与 0.7）
就在这个合约上，同一个 tick（=1）、同一条时段线，新点才**直接可比**。
它的日盘 09:00–10:15 / 10:30–11:30 / 13:30–15:00。

⚠️ 这条实验全程 BUY/OPEN，与 `rb2701` 上那条受保护的空今仓无关，
不会碰到过夜种子。

⚠️ 守卫会在填上的当天变红并说清该做什么，不靠人记得回来看。

#### ✅ 结果（2026-09-09 09:02，`SHFE.ag2702` 日盘内）

    零头 0.400  不是整数倍        零头 0.500  涨停板
    零头 0.450  不是整数倍        零头 0.550  涨停板
                                  零头 0.600  涨停板

边界**恰好**在 0.5，且 0.5 本身落在**接受**侧 —— 与盘后扫出来的完全一致。
那条绊线如期变红并说清了该做什么，已换成**双向**断言
`TestCounterHalfTickBoundaryHoldsInSession`（盘中 48 笔与 `offTick` 逐笔一致）。

⚠️ 顺带确认：这一轮全程 BUY/OPEN，收尾后 **ALIVE 委托 0 条**、
`rb2701` 的过夜种子（多今1/多昨2/**空今1**）原样还在。

⚠️ 顺带记一句方法：这个更正是**机械判出来的**，不是重读文档读出来的 ——
原来那句话读起来完全合理，而「一个点」与「两个跨过边界的点」在散文里
差不多，在判别力上差一整档。

**实验 1 与 2 只需要一个有持仓的账户，不需要本库的任何代码**——所以它们排在最前面，
`cmd/oracle probe` 是嵌套模块里的一个子命令，产出的 JSON 直接成为后续所有版本的离线夹具。

---

## 覆盖缺口（已知，随版本推进逐条消除或转为明确排除）

| 缺口 | 现状 | 计划 |
|---|---|---|
| DCE 日行情端点 | **412 未打通**（probes.md §2） | v0.1.0 尝试另找路径；打不通则在文档里明确标注，由使用者自行提供 |
| 天勤与真实柜台的口径差 | **完全未测量** | v0.8.0 用 SimNow 交叉验并记录差异 |
| 期货公司的保证金加收比例 | 拿不到 | 做成可配置，默认 0，文档写明这会**低估**保证金占用 |
| 套保 / 套利持仓的保证金优惠 | 不做 | 字段第一天就带上，逻辑留到 v1.0 之后 |
| ~~**持仓侧冻结手数的离线对拍**~~ ✅ **已打通（20260909）** | 全量对拍接上了 `Carry + ReplayFrom`：样本 126 → **152**，保证金 14 → **31**，**冻结 0 → 19** —— `volume_*_frozen_*` 三个字段第一次有证据支撑 | ⚠️ 覆盖仍只到 `SHFE.rb2701`：`DCE.m2701` 拿不到交易所结算价（大商所日行情 412 未打通），**缺席就缺席，不拿柜台的 `quotes.settlement` 顶替** —— 那会把独立来源悄悄换成同源。⚠️ 结转样本的失败按**跨日**注册表归类（`conformance.Live()`），与当日样本分开：两群共用一张表的话，表会被撑成「谁都能过」|

---

## 明确排除在 v1.0 之外

见 [design.md](./design.md) §6：期权、组合保证金、套保套利优惠、多币种账户、
实物交割的仓单流转、连续涨跌停的扩板、**强平的执行**。

---

## 排期变更记录

### 过夜种子的保护挪到了下单口上（2026-09-09 实现）

本页上面那句「⚠️ 别在结算前把这些仓平掉：种子没了就要再等一天」，
在 2026-09-09 早晨之前**只是一句话**：全仓唯一一处机器可读的
「账上该有什么」是 `cmd/oracle/probe` 的 `seedPlan`，而它里面
只有两条 `Buy` 腿 —— 那一手**空**今仓一个字都没提到
（它是 reject / frozen 那几条实验顺带开出来的）。

⚠️ 而 `flatten()` 平的正是**今仓**（`volume_short_today`）。
于是任何一条实验的收尾平仓都会把它带走，日志上只写
「已平仓 SHFE.rb2701 1 手」。

处理：新增 `kq.ProtectedLeg`，清单 `protectedLegs` 声明在 `probe` 侧、
由 `kq.Guard.Check` 执行 —— 那是全仓 `InsertOrder` 的必经之路。
⚠️ **不是守在 `flatten()` 上**：另有五个文件直接构造平仓委托，
守在 `flatten` 上会是一道**看起来完整的**闸。

    机制  kq.ProtectedLeg / Guard.Check   五条用例 + 破坏 185–187
    策略  probe.protectedLegs             带 TradingDay，过期自动失效
    接线  TestGuardCarriesProtectedLegs   破坏 184

⚠️ 已知盲区（破坏 188 演示）：`protectedLegs` 清空后接线测试会 `t.Skip`，
接线因此可以在没有保护的日子里静默烂掉。见方法论 56/57。

#### ⚠️ 它与 reject-priority 那条实验**今天**是冲突的

`exp_reject_priority` 的「可平量」违规，做法正是在 `-symbols` 的第一个合约上
发 BUY/`CLOSE` 与 BUY/`CLOSETODAY` —— 而那正好是平掉 `rb2701` 空今仓的动作。
今天在 `rb2701` 上重跑它，那两项会被**本地安全阀**拦下。

⚠️ 危险的不是「拦下来」，是**拦下来之后那条实验原来会照跑**：
`send` 把本地错误文本当作柜台原话存进 `single[]`，而 `single[]` 是归因的**标尺**。
组合单会被同一个阀拦出同一段文本 → 「组合 == 单违规」成立 →
打印一条漂亮的「归因成立」，而这一轮**什么都没问到柜台**。

已修：`send` 多返回一个 `reached`；单违规轮遇到本地拦截**直接报错停下**，
组合轮记 `unusable` 并说明。⚠️ 这条判断**没有自动化测试**（`send` 是闭包，
要一条活的柜台连接），已在代码注释里写明，不装作有。

⚠️ 今天要跑 reject-priority 的话：换一个不碰受保护持仓的合约（`-symbols`），
**不要**去划掉那条保护 —— 种子今天不可再生。



### ✅ 已裁决：`Version` **不从 roadmap 派生**（2026-09-09，使用者裁决）

> 使用者原话：**「可以不从 roadmap 派生，roadmap 只是一个前期工作规划的文档。」**

⚠️ 这条裁决把下面整段争论**关掉了**，而理由比争论本身更干净：
roadmap 是**计划**，不是**事实**。从一份计划里派生出「当前版本是多少」，
等于把一个会随想法改动的文档当成真值源 —— 那正是 A 与 B 两个方案
各自绕不开的东西（A 依赖手工标的 ✅，B 依赖手工维护的例外表）。

**现状因此定为**：`Version` 手工维护，配 `TestNotModeledDebtDistributionIsRecorded`
（改动它会强制看一眼到期分布）。

⚠️ **残留的洞要写明，别当它一起解决了**：那条守卫补的是
「把某处到期版本悄悄往后推」，**不是**「`Version` 自己停着不动」。
后者现在没有任何机制拦 —— 它靠的是发版时有人推一下这个数。
这是一个**已知且被接受**的口子，不是遗漏。

<!-- 历史留档:start -->
以下是裁决前的争论，原样保留。

#### 原「待裁决：`Version` 该不该从 roadmap 派生」

推到 `v0.4.0` **只解决了今天** —— 没有任何东西阻止它在 v0.4.0 上再停两周。
评审方据此提出：**能派生就别维护**。

    提议的规则   roadmap 里**低于** Version 的版本，其点名的包必须全部存在；
                 **等于** Version 的版本，至少有一个包尚不存在

⚠️ **这条规则现在就会红，而且是假阳性。** `v0.1.0` 那一行点名了 `ctperr`，
而 `ctperr` 目录不存在 —— [state.md](./state.md) 的 `packages_done` 里明写着
「`ctperr` 未开始」，理由是 CTP 的数值错误码在这个口子上拿不到
（`simnow_pending#5`）。那不是「版本没做完」，是**这一项被外部条件卡住
而版本仍按其余验收项交付了**。

要让规则成立就得配一张例外表 —— 而例外表**又是一个需要有人记得维护的参数**，
绕回 silent-risks 方法论第 40 条指出的那个形状。

两个候选方向，都还没实现：

    A  判据换成「roadmap 里标了 ✅ 已落地的最高版本 ≤ Version」——
       派生自完成标记而不是包的存在性，绕开 ctperr 这类
       ⚠️ 但现有的 ✅ 标在**条目**上不是整行上，直接用会数错
    B  在版本行里显式标出「本版**不含**的包及理由」，
       让例外表长在**被派生的那份数据**上，而不是长在守卫里

⚠️ 两者都改了「版本算不算做完」的判据，与 `rules_measured` 同属**闸口宽度**
那一类。按同一条理由（想放松它的动机恰好在它挡路时最强），不在无人值守时定。

⚠️ **评审方随后不推荐 A 与 B**，理由比上面这条更根本：

> A 依赖手工标的 ✅、B 依赖手工维护的例外表 ——
> **两者都把「谁来保证这个参数是新的」推给另一个手工参数**，
> 搬走了过期，没有消灭它。

于是采纳了第三条（评审方提的 C），它**绕开「做完是什么意思」这个问题**：
不去保证 `Version` 是对的，而是保证**改动它这个动作会强制看一眼到期分布** ——
`TestNotModeledDebtDistributionIsRecorded` 入库一份到期分布金文件，
与实算逐档比。⚠️ 采纳前核过它**不重新定义「做完」**，因此不属于闸口宽度；
它真正补的洞是「把某处到期版本悄悄往后推」，而那一件**现有那条测试
禁止了却发现不了**（破坏 166）。
<!-- 历史留档:end -->

⚠️ 评审方同时判断这件事**今天不急**：最近的一档是 v0.8.0 而当前是 v0.4.0，
机制已被证明会响、分布也打印出来了，所以 `Version` 停住的代价目前有界且可见。
A/B 等到真要推向 v0.8.0 时再定。

### 2026-09-09：实时对拍能落盘了；而落盘当场暴露出它**从来没有行情**

⚠️ 导出面变更（门禁④）：`probe.WriteFixtureJSON` 新增；
`oracle conformance` 新增 `-dump`（⚠️ 无默认值，落盘是一次显式的决定）。

来历是评审要闭合 tag 门槛 (b)：代码路径那一半他核过了（一份实现两个数据源），
另一半「它到底连上去跑过没有」要**产物**而不是转述 ——
「我验产物，不验你的转述」对实时那条路同样成立。

⚠️ 而把那份截面落进夹具树的当场，**缺行情那条棘轮把它顶了出来**：

    实时截面的 quotes 段是**空的**
    根因：LiveFixtureJSON 里连行情那一步写着 `if len(symbols) > 0`，
          而实时对拍不带 -symbols 跑时 symbols 是空的 —— 于是一个行情都不连，
          **且没有任何东西说这件事**：昨结算价缺席只表现为「那几个字段比不了」

修完（无条件连行情 + 落盘前补订出现过的合约）之后，同一条命令的
「对得上且被触发过」从 **20 涨到 23**、「本库还没实现」从 14 降到 11 ——
⚠️ 那三个字段一直是能比的，只是**没有输入**。

⚠️ 这是 silent-risks 方法论 38 的第二次现场：采样缺口修了 `dump` 那条路，
**漏了 LiveFixtureJSON 这条**。同一个缺口两份代码，我只修了一份。

### 2026-09-09：`Version` 在 v0.1.0 上停了两天，而到期机制因此一直空转

design.md §5 要求每一处「不建模」带到期版本，`TestNotModeledDeclarationsHaveNotExpired`
是那个机制：到期版本**不晚于** `Version` 就红。

⚠️ 而 `Version` 从 09-07 一直是 `v0.1.0`。到期版本只要晚于它就不会红，
`Version` 不动，**任何声明都晚于它** ——

> **一个「到期机制」在它自己的基准不动时，与没有这个机制完全一样。**

推到 `v0.4.0`（v0.1.0/v0.2.0 已合 main 且验收达成；v0.3.0 的验收已达成、
清单上剩的两项是被这个口子堵死的；v0.4.0 的报单校验与冻结已落地）。
现存声明是 v0.8.0 与 v1.0.0，因此这次推进**一条都没到期** ——
⚠️ 也就是说：机制仍然没有被真正走过，只是基准不再是假的。

顺带修掉守卫自己的一个洞：反向那一支（「包文档里不该说别的版本在开发中」）
的候选表里**含有当前版本**，于是 `Version` 一往前推，守卫就把正确的包文档
判成冲突。⚠️ 一条在「该动的东西终于动了」时才报警的守卫，会让人把它改回去。

同批更正三处过期陈述：

<!-- 历史留档:start -->

    doc.go 的包注释   「本包目前只有包声明与文档守卫，核算逻辑尚未落地」
                      ⚠️ 它是这个库**最公开**的一句话（go doc / pkg.go.dev），
                      却躲过禁语扫描两天 —— 扫描只看 README.md 与 docs/*.md

<!-- 历史留档:end -->

    v0.3.0 清单       三项里一项已做、两项是「做不了」而非「没做」
    v0.4.0 清单       order 那一半已落地；match 的前提不成立

禁语扫描范围加上 `doc.go`（只加它，不加全部 `.go`：那会把「禁语」变成
「禁词」，而代码注释里讨论这些字符串是正当的）。禁语表 11 → 13 条。

### 2026-09-09：拒绝优先级实测，v0.4.0 的验收行要改口径

v0.4.0 的验收写的是「被拒报单的**错误码**一致」。⚠️ 那条今天做不到，
而做不到的理由与「还没做」是两回事：快期模拟回的是**中文文案**，
CTP 的数值错误码要等 SimNow（simnow_pending#5）。

今晚补上的是同一块里**能**验证的一半：**拒因是哪一项**。
柜台只回一个 `last_msg`，于是「同时违反两项时报哪一个」是可测的，
而它此前**一次观测都没有** —— 顺序整个抄自 cn-futures-rules.md §9 的文档表。

实测结果：文档表是对的（最小变动价位 > 涨跌停 > 可平量）。
⚠️ **但我先得出了相反的结论，并真的改了代码。** 全过程写在
[probes.md](./probes.md) §14；一句话版本是：构造「不是整数倍」用的零头
0.7 个 tick，而柜台**不把它当偏离**，那笔单只违反了一项。

⚠️ **两件事比结论本身重要：**

    对调之后**全套测试照样绿** —— 八项的相对顺序此前是一段
    **没有任何守卫**的语义，而它在代码里长得和被守卫的部分一模一样

    实验里那层「违规真的构造出来了吗」的零层核对**没拦住**，
    因为它用的是**本库的**判据，而分歧恰恰在判据本身

现在有 `TestPriceTickBeforePriceLimit`、`TestRejectPriorityFromFixtures`、
`TestCounterTickToleranceIsHalfATick` 三条守卫，破坏用例 144/145/148。

⚠️ 验收行的口径因此拆成两半，并**分别注明能不能做**：

    拒因是哪一项    可测，已开始；覆盖了哪几对见 state.md 的
                    `reject_priority_measured`（⚠️ 验收行里**不抄数**）
    拒因的数值码    ⚠️ 这个口子上**测不到**，等 simnow_pending#5

⚠️ 上面这一行 20260909 被抓到还在抄旧数（写着「3 对」而实测已 4 对）。
单一来源那一轮扫干净了代码注释与事实表，**唯独漏了验收行** ——
而验收行正是这个数最要命的地方：**它定义「做完了」是什么意思**。
⚠️ 上一次（v0.1.0/v0.2.0 的两条验收行）也是同一个位置。

顺带带出一处**已知偏离**：快期收下零头 ≥ 半个 tick 的报价，而交易所侧
任何非整数倍都要拒。本库按交易所口径一律拒绝，记 simnow_pending#11。

### ✅ 已裁决：`match` **不做盘口，默认 100% 全量成交**（2026-09-09，使用者裁决）

> 使用者原话：**「不做盘口，文档要说明好，默认下单就是模拟 100% 全量成交。」**

于是本页下面那场「要不要把 bid/ask 纳入白名单」的争论**关掉了**：
`quoteKeep` 保持现状，不留盘口。

#### ⚠️ 两个偏离维度，**方向相反** —— 这是这条裁决最要紧的一句

实测（20260909）：可成交的限价单**成交在对手价上**，不在自己的报价上。

    BUY/OPEN   报 3321（涨停）→ 实际成交 3166
    SELL/OPEN  报 3004（跌停）→ 实际成交 3165

本库按**自己的报价**成交。于是：

    价格维度      对可成交单，本库拿到的价格**不如实盘**
                  （买贵了、卖便宜了）→ **保守**

    成交与否维度  本库假定**立刻 100% 全量成交**
                  ⚠️ 而挂在远离市场的限价单，实盘上**可能永远不成交**
                  → **乐观**，且这是危险的那一头

> ⚠️ **不能笼统说「保守」或「乐观」** —— 两个维度方向相反，
> 而只说其中一个的人，会按那一个去调整自己的判断。

⚠️ 危险的是第二个：策略会看到它**实际拿不到的成交**，而账面数字全程合理、
不报错。已作为第 9 条静默形态记进 [silent-risks.md](./silent-risks.md)。

#### ⚠️ 这条裁决**不能**被测试验证，只能被声明

夹具不留盘口，于是「这一笔在实盘会不会成交」在语料里**没有观测**。
⚠️ 也就是说：`match` 的成交价逻辑写成什么样都不会红。
**这不是「以后补测试」，是「这个口子上永远补不了」** ——
要验它得先有盘口，而不留盘口正是这条裁决本身。

于是要求落在实现上：`match` 落地时必须**在代码里显式标注**这两个方向，
并让「全量成交」这个假设出现在**导出面的文档注释**里，而不是埋在实现中。

---

<!-- 历史留档:start -->
裁决前的分析，原样保留。

#### 原「2026-09-09：`match` 的前提条件不成立 —— 盘口不在夹具里」


v0.4.0 的另一半是 `match`（限价/市价、涨跌停、最小变动价位、成交角色判定）。
今晚没做，理由不是没时间，是**核心行为在当前证据上不可验证**。

实测（20260909）：

	BUY/OPEN   报 3321（涨停价）→ **成交在 3166**
	SELL/OPEN  报 3004（跌停价）→ **成交在 3165**
	同一时刻 last_price = 3165

也就是说可成交的限价单**成交在对手价上，不在自己的报价上**。要建模它就得知道
盘口 —— 而 `quoteKeep` **刻意不留** bid/ask：那段注释写着理由，
「五档盘口与成交量每一拍都在动，存进夹具只会让同名夹具永远『内容不同』，
而那条冲突守卫会因此天天报警 —— 一个天天报警的守卫等于没有守卫」。

⚠️ 于是这是一个**要人来裁的取舍**，不该由一次无人值守的实现顺手改掉。
⚠️ 而措辞要准（评审方 20260909 更正）：不是「match 不可验证」，
是「**在当前夹具白名单下**不可验证」—— `quoteKeep` 不留 bid/ask 是
**我们做的一个选择**，不是外部约束。写成前者读起来像没有出路，
而出路在我们自己手里。真实的两个选项是：

	把 bid/ask 纳入白名单   match 可验证；代价是冲突守卫的信噪比，
	                        且要说明为什么盘口不含标识信息
	不写 match              成交价逻辑写成什么样都不会红（没有观测）

⚠️ 在裁决之前**不写 match 的成交价逻辑**：一个无法被验证的实现，
与一个正确的实现在测试上长得一模一样，而它会以「v0.4.0 做完了」的形式
留在清单上。

（`match` 的另外几项 —— 涨跌停、最小变动价位 —— 已经在 `order` 的八项校验里，
它们是**能**验证的：涨跌停有 refdata.PriceLimits 与实测的两家取整方向。）
<!-- 历史留档:end -->

### 2026-09-09：`order` 的八项校验落地（v0.4.0 的一半）

#### 导出面（门禁④，**含枚举取值**）

⚠️ 这一段是 20260909 送审前补的：上面那条变更记录写了**设计**，
却没有像 09-07/09-08 那几条一样**逐个列出导出的标识符** ——
而门禁④ 查的正是这个，且明写「含枚举取值」。
**记了设计不等于记了导出面**，两者在变更记录里长得像。

**主模块 `order`（新包）**：

    类型   Request / Facts / Result / Rejection / Unchecked
           Frozen / FreezeInput / Book
    函数   Validate(Request, Facts) Result
           FreezeOf(Request, FreezeInput) (Frozen, error)
           NewBook() *Book
    方法   Result.OK() / Result.FullyChecked() / Result.String()
           Frozen.Add() / Frozen.IsZero()
           Book.Insert() / Remove() / Total() / TotalOf() / Live()
           Check.String()

**枚举 `order.Check`（`uint8`）—— 取值与顺序都是导出面的一部分**：

    CheckUnknown = iota   CheckTradable      CheckSession
    CheckPriceTick        CheckPriceLimit    CheckVolumeRange
    CheckClosable         CheckFunds         CheckPositionLimit

⚠️ **顺序本身有语义**：它就是拒绝优先级（cn-futures-rules.md §9），
`allChecks` 与它同序，守卫 `TestRejectionPriorityMatchesCounter`。
⚠️ 20260909 我曾据一次坏测量把 `CheckPriceTick` 与 `CheckPriceLimit` 对调过，
当天回退（破坏 144/145 钉住）—— **改这个顺序是改导出面语义，不是改实现**。

**主模块 `view.Position` 新增字段**：`FrozenLongToday` / `FrozenLongHistory` /
`FrozenShortToday` / `FrozenShortHistory` / `HasFrozen`。
⚠️ `HasFrozen` 为假时那几个字段渲染成 `NotImplemented` 而不是 0 ——
区分「没有挂单」与「没接冻结数据」，后者拿 0 去比会比出空洞的一致。

**主模块 `conformance` 新增**：`KnownClass` / `Registry` / `CrossDay()` / `Live()`。

**主模块 `conformance/fixture` 新增**：`Notify` / `NakedClosePolicy`（枚举，取值 **`NakedCloseRefuse`（零值）**
与 `NakedCloseIsYesterday`）/ `FrozenOf` / `FrozenAccountOf` /
`LiveOrderSymbols`。

⚠️ `NakedClosePolicy` 的**零值是「拒绝」**，不是任何一种语义 ——
默认值会被沿用而不被注意到，而这里选错一边会让平今/平昨整个错位。
⚠️ 我在写上面这份清单时**第一遍就把这个枚举的取值写错了**（写成 `NakedCloseIsToday`，那个标识符根本不存在）——
这正是门禁④ 要「含枚举取值」的原因：**导出面里最容易写错的就是取值**，
而错了的清单比没有清单更糟，它看起来像核对过。

**嵌套模块 `cmd/oracle`**：`kq.ProtectedLeg`（新类型，字段 `Symbol`/`Direction`/
`TradingDay`/`Why`）、`kq.Guard` 新增字段 `Protected` / `TradingDay`、
`kq.Notify` 新增 `Content`、`probe.WriteFixtureJSON`。

八项校验按 cn-futures-rules.md §9 的**拒绝优先级**实现。⚠️ 冻结与撤单没做，
`match` 也没开始 —— v0.4.0 仍未完成，这里只记已落地的那一半。

设计上有一条比八项本身更要紧：**跑不了的校验不许当成通过**。
八项里有几项需要本库拿不到的输入（手数上下限、限仓、交易时段），
而一个「拿不到就跳过」的校验器在缺输入时会返回「通过」——
那个「通过」与真的全部查过长得一模一样。

所以 `Validate` 返回 `Result{Rejected, Unchecked}`，`OK()` **要求全部查过**。
⚠️ 一笔「因为查不了所以放过」的单，会让回测开出实际开不出的仓
（silent-risks.md 第 7 条）。

另外两条：**跑完八项再返回**（短路会让「还有几项没查成」取决于第一个拒绝
出现在第几位），**拒因只报优先级最高的那个**（柜台只回一个 last_msg，
乱序会让两边都判「拒绝」而拒因不同 —— 差异不会以失败的形式出现）。

### 2026-09-09：`cmd/oracle` 开始依赖主模块（实时对拍落地）

`oracle conformance` 此前只是个占位（打印「还没有实现可对拍」），
而评审把它列成打 tag 的门槛之一。实现它要一个决定：**比对逻辑写在哪。**

选了**让嵌套模块依赖主模块**（`replace ../..`），理由只有一条：
比对逻辑写两份，两份就会漂移，而漂移的表现是
**同一个柜台在两条路上给出不同的判定**，且谁都不报错。
现在实时那条路把截面 marshal 成夹具 JSON，交给主模块的
`fixture.Load` 解析，再走同一套 `ComparePosition` / `Classify` ——
夹具格式漂移会**同时**打断两条路，而不是只打断没人在跑的那一条。

⚠️ 这**不破**「主模块的依赖树只有 decimal 一个」那条硬约束：
方向是 oracle → 主模块，而使用者 `go get` 拿到的是主模块。
反过来才会破。⚠️ 那条约束此前**没有任何守卫**，现补
`TestMainModuleHasOneDependency`（它自己的破坏验证有个洞，如实记在注释里）。

同批：已知差异的归类表从 `crossday_test.go` 挪进
`conformance/registry_crossday.go`（非测试代码）——同样是「两条路要用它」。
⚠️ 只有测试有它的话，实时那条路会把每一处已知差异都报成失败，于是**永远红**，
而一个永远红的对拍工具和一个永远绿的一样会被无视。

### 2026-09-09：`settle` 包并入 `position`（评审 F4）

design.md §3 的包树里画着 `settle/`，v0.2.0 的验收行也写着「`settle` 的**完整**
日终链路」——而实际实现是 `position.Settle`，**没有 `settle` 目录**。

⚠️ 这次合并本身是对的（理由见 design.md 那一格），错的是**没记这一条**：
design.md 自己的规矩写着「包边界在 v1.0.0 之前可以调整……就合并它，
**并在 roadmap.md 里记一条变更**」。

⚠️ 它与 `order/` `match/` `risk/` 不是一回事：那三个也画着但不存在，
而它们是**合法的未来版本**（v0.4.0 / v0.4.0 / v0.6.0）。`settle` 的
**职责已经在别处实现了**，那才叫漂移。

守卫：`TestPackageTreeMatchesReality` —— 解析 design.md 的包树与
`go list ./...` 对照，画了但不存在的包必须在 roadmap 里查得到目标版本。


| 日期 | 变更 | 原因 |
|---|---|---|
| 2026-09-07 | 初稿 | — |
| 2026-09-07 | **导出面设计变更**：`types` 的枚举取值由「与 CTP 线格式一致」改为**规范化**，CTP 与 DIFF 两套映射在包内显式成对给出并配往返测试。原因是本库实际对接两个口子而线格式不同（CTP 用单字符、DIFF 用 `BUY`/`OPEN` 这类词），挑其中一套等于默默偏袒一个口子，另一个的转换会散落到调用处——而散落的转换没有任何一处能被完整检查 | 写 `types` 时发现与 design.md §3 的原描述冲突 |
| 2026-09-07 | **文档阶段结案抽查通过。** 另收两条 v0.1.0 要落地的机制：① 单一状态源 `docs/state.md` + 由状态键派生的禁语检查（治「改状态改不干净」）—— ⚠️ 但它是**对自然语言的黑名单**，只防复发不防首发，故禁语须从状态翻转那次提交里**被删掉的字符串**中生成，且这一边界要写进检查的注释；**另加一半「减少复述面」治首发**——其他文档不断言状态只给链接，白名单建在**位置**（有限可枚举）而非措辞（无穷）上，两条基于不同原理故不会一起失效。当前散落面 23 处，已量化可查 ② 夹具脱敏用**白名单**而非黑名单：黑名单漏一个字段是静默泄漏，白名单漏一个是「缺字段」报错。另修 `probes.md` 待办标题仍为现时语气 | 与「到期版本」「三态验收」同形状：事实有唯一的家，复述它的地方缺一条链接 |
| 2026-09-07 | **导出面变更**：`CommissionRates` / `MarginRates` 由 `fee` / `margin` 迁至 `refdata`，原处保留类型别名。原因是它们是**规则数据**而非计算逻辑，定义在 `fee`/`margin` 里会让 `refdata` 返回费率时反过来 import 那两个包，而依赖图是 `refdata ← {fee, margin, pnl}`——**成环**。别名保证调用方两种写法都通，非测试代码零处受影响 | 写 `refdata` 时被依赖方向逼出来 |
| 2026-09-08 | **导出面变更（嵌套模块 `cmd/oracle`，非主模块 API）**：① `kq.Secret{Name, Value}` 新增，`kq.Scrubbed` 的第二参由 `[]string` 改为 `[]Secret`，`probe.Env.Secrets()` 返回类型随之改变——命中时能报「`KQ_PASSWORD` 出现在夹具里」而不是「某个长度 12 的值」，键名不是秘密而可操作性差很多；② `kq.BlindSpots` 新增，把「独立复查查不了哪些值」变成可调用的声明，理由是原先对短值的跳过是**静默**的，「没报错」会被读成「都查过了」；③ `kq.Guard.Check` 的**行为**变更：`MaxVolume` 只对 `Offset == Open` 生效。⚠️ 这一条是行为变更而非签名变更，金文件守卫抓不到——起因是一个防止扩大风险的守卫把平仓单也挡了，导致 2 手仓平不掉 | 夜盘实测时被真实故障逼出来 |
| 2026-09-08 | **命令行默认值变更**：`cmd/oracle` 的 `-dump` 默认值由 `"testdata/probes"` 改为空串。⚠️ 原默认值让 `Runner` 里「`DumpDir` 为空才回落到 `.env`」**永远不成立**，`.env` 的 `PROBE_DUMP_DIR` 从来没被读过；我为修「相对路径另开夹具树」去改 `.env`，改完毫无效果，唯一发现这件事的是机械守卫 `TestNoStrayFixtureTrees`。**一个什么也没改的修复，在日志里和一个生效的修复长得一模一样** | 同上 |
| 2026-09-08 | **行为变更（嵌套模块 `cmd/oracle`）**：`kq.Guard.Check` 的手数上限判据由「是开仓才拦」翻成「不是**已识别的平仓**就拦」。⚠️ 原写法是个洞：`Offset` 底层是 `string`，零值 `""` 不等于任何常量，实测 `""` / `"BUYOPEN"` / `"open"` / `"CLOSE_TODAY"` **全部绕过上限**——而这是真账户上的安全阀，失败方向朝着「不拦」。翻转后未识别的取值从「不拦」变成「多拦一次」，将来加 `CloseYesterday` 漏改这里也只会多拦。已补 `TestGuardMaxVolumeFailsSafe`（带对照组，双向破过） | 评审 F1 指出，本方独立复现并扩出两个新反例 |
| 2026-09-08 | **行为变更（嵌套模块 `cmd/oracle`）**：`probe.flatten` 的 CLOSETODAY→CLOSE 回退。⚠️ 原判据是「30 秒内没全成就回退」，而注释写的是「**被拒**则回退」——**注释描述了一个代码里并不存在的条件**。差别在有昨仓时是致命的：实测（快期模拟·语义，§9）`CLOSE` 在 `UseHistory` 交易所上被解释为**平昨**，于是一次「限价没被打到」的超时会去平掉过夜种子，而日志只说「换一种开平标志再试」——**它不是没平掉，它是平了别的**。三条修法：① 只在终态未全成（被拒）时回退，超时不算；② 超时先撤单再返回，否则两笔可能都成交；③ **只要该方向有昨仓就禁止 CLOSE 回退**，不看拒因，只看账上有没有昨仓。判定抽成纯函数 `decideFallback` 并穷举断言，三方向破过 | 评审在开盘前指出；本方核实其依据（§9 的五条实测拒因）后确认 |
| 2026-09-08 | **导出面新增（主模块 `refdata`）**：交易日历与时段 —— `ClockTime` / `NewClockTime` / `MustClockTime` / `Session` / `SessionTable` / `Calendar` / `NewCalendar` / `CNZone`。核心操作是 `TradingDayAt(t, 交易所, 品种)`，一次**查表**而不是一次推算。⚠️ 三处刻意的「报错而不猜」：落在任何时段之外报错（收盘到夜盘之间不属于任何交易日，返回「最近的那个」会让「引擎在非交易时段推进」没有动静）；`NextTradingDay` 超出日历覆盖范围返回 false 而不外推；`NewClockTime` 越界报错而不取模。⚠️ 时区用固定 +08:00 而非 IANA，已知边界（1991 年前夏令时会算错）由 `TestZoneIsFixedNotIANA` 钉住 | v0.1.0 的 refdata 范围里「交易日历与时段」那一块 |
| 2026-09-08 | **导出面新增（主模块 `refdata`）**：快照持久化 —— `Evidence`（四档证据等级）/ `LoadOptions` / `Snapshot.Save` / `Load`。⚠️ 三处刻意：小数一律序列化成字符串（JSON 数字过一次 float64 就不再是原来的数）；加载路径**重新走一遍 `Builder`**（快照文件可手改，直接塞字段会让构造期校验在加载路径上失效）；`placeholder` 默认不许加载（空快照能加载成功时，调用方拿到的是「查不到这个合约」而不是「还没有参考数据」）。⚠️ `Save` 的 `generatedAt` 由调用方传入，函数内不调 `time.Now()` —— 否则「两次生成是否字节相同」无法被测试回答 | v0.1.0 的 refdata 范围里「快照持久化」那一块 |
| 2026-09-08 | **新包（主模块 `conformance`）**：双口子对拍的**判定**机制 —— `Verdict` / `Field` / `Deviation` / `Report` / `Classify`。⚠️ 在 design.md §5 的三档之外新增**第四档「已知口子差异」**：按额手续费的基准价上，本库按 CTP 建模（成交价）而快期实测用昨结算价，两边各自都对、值却不同，按三档只能判失败。第四档带**三条硬门槛**（出处 / 裁决者 / 本库选了哪边及理由），缺一即判失败 —— **一个可以随口声明的豁免档，比没有这一档更坏**。⚠️ `Untriggered` 与 `KnownDeviation` 都**不算通过** | v0.1.0 验收标准的落地 |
| 2026-09-08 | **新包（主模块 `view`）**：与柜台业务截面字段级同构的视图 —— `Presence` / `Value` / `Account` / `AccountOf` / `CoverExactly`。⚠️ 核心不是字段映射，是**值的三种状态**：有值 / 明确不建模（带到期版本）/ **还没实现**。第三种是本包存在的理由：一个还没实现的字段若渲染成 0，而柜台那边恰好也是 0（空仓、无期权、无冻结时大量字段都是 0），对拍会判「一致」—— **一个缺失的实现伪装成了一致**，那比算错更坏。⚠️ 字段集与**夹具实测的 21 个**逐一比对（不是手抄名单：手抄的名单会和视图被同一个人一起改错），两个方向都查 | v0.2.0 的 view，提前落地以服务今晚的对拍 |
| 2026-09-08 | **新包（主模块 `refdata/live`）**：从天勤公开合约字典拉规则数据 —— `FetchSymbols` / `ToInstrument` / `ToSessionTable` / `SessionTablesOf`。独立成子包是因为它引入 `net/http`（stdlib，第三方依赖仍只有 `shopspring/decimal`）。⚠️ 那份文件 **334 MiB**，所以流式解析、**单次请求、不续传**（拼装才是静默丢失的来源），且 206 视为错误而非可用结果。⚠️ `PositionDateType` / `MaxMarginSide` / `PriceLimitRatio` 字典里没有，**留零值不猜** —— 零值在 refdata 里是「规则数据缺失，使用即报错」，一个「差不多能用」的规格会在平今平昨和大边上静默算错 | v0.1.0 的 `refdata/live`；字典里的 `trading_time` 能把 `Calendar` 里我照文档填的时段表换成真数据 |
| 2026-09-07 | **v0.1.0 代码批评审「有条件通过」（不合并 main）。** 评审实测出一个洞：`margin` 的实验 1 守卫可被空 `legs` 绕过——`scope` 是前置检查而 `basis` 只在逐 leg 循环里才被消费，`len(legs)==0` 的早返回在它之前。⚠️ 零值报错的意图不是算对数，是告诉调用方它没配这个参数；空仓放行会让**配置错误与它的报告点错开在不同时间、不同位置**。已前置并抽成共用的 `check()`，另加跨包守卫 `guards_test.go` 把这一类一次性堵住 | ⚠️ **评审方主动声明了覆盖边界**：7764 行新增代码**未逐行读**，做的是五个未实测点的穿透测试 + 门禁 ③④ 机械核对 + ① 措辞扫描与定点阅读 + ② 一处定点双向破坏。所以「通过」的准确含义是**它实际验过的那几条性质成立**，不是「这批代码是对的」——后者要柜台对拍，而 `rules_measured = 0`。下一批**按包送审** |
| 2026-09-07 | **复核轮「有条件通过」，据此改 4 处 + 采纳 2 条机制**：C1 清掉三处过期的证据等级陈述（`probes.md` §5 结尾仍写「尚未实际登录过」、`cn-futures-rules.md` 表头理由与 §1 表里 SimNow 仍「待注册」；这些字符串已进 [state.md](./state.md) 的派生禁语表）——⚠️ **一个自相矛盾的证据等级系统，读者无法判断该信哪一句，整套分级就此贬值**；C2 验收由两态改**三态**，堵零值假通过；C3 `cmd/probe` 改名残留三处（v0.1.0 那一行是实现时照着建目录的那一行，照它建目录 B1 会原样回来）；C4 `oracle` 加注 test oracle。另采纳两条：不建模声明与其到期校验必须同提交；字段集断言「⊆ 已知集」，多出即判未分类 | 评审方独立复核实测结论与凭据泄漏 |
| 2026-09-07 | **第一轮文档评审「有条件通过」，据此改 6 处**：① `cmd/probe` 并入 `cmd/oracle` 嵌套模块（原方案会破掉主模块依赖硬约束，且破得没有报错）② 实验 4 的影响面由「手续费」扩为 `fee`+`pnl`+`account`，判别方法追加 `CloseProfitByDate` ③ 新增「不建模必须带到期版本」机制，列为 v0.2.0 交付物 ④ 今昨仓划分与滚动由 v0.3.0 前移到 v0.2.0（它是 `Settle` 第 7 步，且逐日盯市的盈亏基线本就依赖今昨划分，放在 v0.3.0 会让 v0.2.0 的 `Settle` 是半程的）⑤ 依赖 DAG 补 `ctperr` 与 `internal/decimalx` ⑥ 账户对拍字段数由 18 改为 **23**（实测比 DIFF 文档多 5 个） | 评审方独立核对四项门禁 |
| 2026-09-07 | 对拍口子由「SimNow 单口」改为「天勤快期模拟（主）+ SimNow（权威裁决）」双口 | 使用者指出已有快期账户。**实测验证**后发现它比原方案更好：DIFF 是 JSON over WebSocket，**纯 Go 可达、不需要 cgo**，且账户与持仓截面原生区分「按开仓价」与「按昨结算价」两个基线。但它给不了两套平仓盈亏口径与完整费率表，故 SimNow 保留为权威裁决口 |
